# (c) 2026 Firmansyah Nainggolan <firmansyah@nainggolan.id>. All Rights Reserved.
# SPDX-License-Identifier: APACHE-2.0

## =============================================================================
## @section Global parameters
## -----------------------------------------------------------------------------
## Global Docker image parameters shared across this chart and any dependencies.
## Currently available: imageRegistry, imagePullSecrets, storageClass, and
## platform compatibility adaptations (OpenShift, SELinux).
## =============================================================================

## @param global.imageRegistry Global Docker image registry
## @param global.imagePullSecrets Global Docker registry secret names as an array
## @param global.storageClass Global StorageClass for Persistent Volume(s)
##
global:
  imageRegistry: ""
  ## E.g.
  ## imagePullSecrets:
  ##   - myRegistryKeySecretName
  ##
  imagePullSecrets: []
  storageClass: ""
  ## Compatibility adaptations for Kubernetes platforms
  ##
  compatibility:
    ## Compatibility adaptations for Openshift
    ##
    openshift:
      ## @param global.compatibility.openshift.adaptSecurityContext Adapt the securityContext sections of the deployment to make them compatible with Openshift restricted-v2 SCC: remove runAsUser, runAsGroup and fsGroup and let the platform use their allowed default IDs. Possible values: auto (apply if the detected running cluster is Openshift), force (perform the adaptation always), disabled (do not perform adaptation)
      ##
      adaptSecurityContext: auto
    ## @param global.compatibility.omitEmptySeLinuxOptions If set to true, removes the seLinuxOptions from the securityContexts when it is set to an empty object
    ##
    omitEmptySeLinuxOptions: false


## =============================================================================
## @section Common parameters
## -----------------------------------------------------------------------------
## Generic chart-wide parameters that affect every rendered resource:
## naming, labels/annotations applied to all objects, cluster domain,
## arbitrary extra manifests, and diagnostic-mode overrides.
## =============================================================================

## @param kubeVersion Force target Kubernetes version (using Helm capabilities if not set)
##
kubeVersion: ""
## @param nameOverride String to partially override freeradius.fullname
##
nameOverride: ""
## @param namespaceOverride String to partially override freeradius.namespace
##
namespaceOverride: ""
## @param fullnameOverride String to fully override freeradius.fullname
##
fullnameOverride: ""
## @param commonLabels Labels to add to all deployed objects
##
commonLabels: {}
## @param commonAnnotations Annotations to add to all deployed objects
##
commonAnnotations: {}
## @param clusterDomain Default Kubernetes cluster domain
##
clusterDomain: cluster.local
## @param extraDeploy Array of extra objects to deploy with the release
##
extraDeploy: []

## Enable diagnostic mode in the deployment
##
diagnosticMode:
  ## @param diagnosticMode.enabled Enable diagnostic mode (all probes will be disabled and the command will be overridden)
  ##
  enabled: false
  ## @param diagnosticMode.command Command to override all containers in the deployment
  ##
  command:
    - sleep
  ## @param diagnosticMode.args Args to override all containers in the deployment
  ##
  args:
    - infinity


## =============================================================================
## @section FreeRADIUS Image parameters
## -----------------------------------------------------------------------------
## The Docker image used to run FreeRADIUS. Override these to pin a specific
## version, point at a private registry, or change the pull policy.
## =============================================================================

## FreeRADIUS image
## ref: https://hub.docker.com/r/freeradius/freeradius-server/tags
## @param image.registry FreeRADIUS image registry
## @param image.repository FreeRADIUS image repository
## @param image.tag FreeRADIUS image tag (immutable tags are recommended)
## @param image.pullPolicy FreeRADIUS image pull policy
## @param image.pullSecrets Specify docker-registry secret names as an array
## @param image.debug Specify if debug logs should be enabled
##
image:
  registry: docker.io
  repository: freeradius/freeradius-server
  tag: "3.2.8"
  ## Specify a imagePullPolicy
  ## Defaults to 'Always' if image tag is 'latest', else set to 'IfNotPresent'
  ## ref: https://kubernetes.io/docs/user-guide/images/#pre-pulling-images
  ##
  pullPolicy: IfNotPresent
  ## Optionally specify an array of imagePullSecrets (secrets must be manually created in the namespace)
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
  ## Example:
  ## pullSecrets:
  ##   - myRegistryKeySecretName
  ##
  pullSecrets: []
  ## Set to true if you would like to see extra information on logs
  ##
  debug: false


## =============================================================================
## @section FreeRADIUS Configuration parameters
## -----------------------------------------------------------------------------
## FreeRADIUS application configuration: architecture mode, custom radiusd.conf
## content, init scripts, mods-enabled and sites-enabled module config, RADIUS
## clients map, and extra environment variables / startup flags.
## =============================================================================

## @param architecture FreeRADIUS architecture (`standalone` or `replication`)
##
architecture: standalone

## FreeRADIUS `log { }` directives surfaced as values. Renders into the
## chart-managed `radiusd.conf` (`templates/configmaps/configuration.yaml`)
## when neither `configurations` nor `configurationsConfigMap` overrides it.
## Field names mirror FreeRADIUS native directive names (snake_case).
## Booleans below render as FreeRADIUS-native `yes` / `no`.
##
## @param logging.destination Where log lines go: `files` (write to `logging.file`), `syslog` (use `logging.syslog_facility`), `stdout`, or `stderr`. The `-X` debug flag overrides this to stdout at runtime regardless.
## @param logging.colourise Highlight WARN / ERROR lines on stderr / stdout. No-op when output is not a TTY.
## @param logging.file Log file path when `destination: files`. Defaults to `${logdir}/radius.log` (interpolated by FreeRADIUS at startup using the `logdir` from radiusd.conf, not by Helm).
## @param logging.syslog_facility Syslog facility used when `destination: syslog`. Allowed values are OS-dependent; `daemon` is the conventional default.
## @param logging.stripped_names When `false` (default), log the raw `User-Name` attribute; when `true`, log the stripped form. The chart default matches FreeRADIUS's upstream default.
## @param logging.auth Log every Access-Accept / Access-Reject result.
## @param logging.auth_badpass Log the offered password when authentication is rejected. Requires `auth: true`. Off by default — secrets in logs are a footgun.
## @param logging.auth_goodpass Log the offered password when authentication succeeds. Requires `auth: true`. Off by default — secrets in logs are a footgun.
## @param logging.msg_denied Reply message returned when the user exceeds the Simultaneous-Use limit. Rendered as a quoted string.
##
logging:
  destination: files
  colourise: true
  file: ${logdir}/radius.log
  syslog_facility: daemon
  stripped_names: false
  auth: false
  auth_badpass: false
  auth_goodpass: false
  msg_denied: "You are already logged in - access denied"

configurations: ""

## @param configurationsConfigMap Name of an externally-managed ConfigMap supplying FreeRADIUS configuration (must contain a `radiusd.conf` key). When set, the chart skips rendering its own `<fullname>-configurations` ConfigMap; the Deployment mounts this resource at `/etc/freeradius/radiusd.conf` instead. The value is evaluated as a template.
##
configurationsConfigMap: ""
## @param existingConfigmap Name of an existing ConfigMap supplying the chart's standard env-vars (in place of the chart-rendered `<fullname>-envvars`). When set, the chart skips its own envvars ConfigMap and the Deployment's `envFrom` references this resource.
##
existingConfigmap: ""
## @param extraStartupArgs Extra default startup args (prepended to the container args)
##
extraStartupArgs: ""
## @param extraFlags FreeRADIUS additional command line flags
##
extraFlags: ""
## @param initdbScripts Dictionary of initdb scripts run at first boot. Alternatively, drop files under `files/docker-entrypoint-initdb.d/`.
## e.g:
## initdbScripts:
##   my_init_script.sh: |
##      #!/bin/sh
##      echo "Do something."
##
initdbScripts: {}
## @param initdbScriptsConfigMap ConfigMap with the initdb scripts (Note: Overrides `initdbScripts`)
##
initdbScriptsConfigMap: ""
## @param extraEnvVars Extra environment variables to be set on FreeRADIUS containers
## E.g.
## extraEnvVars:
##  - name: TZ
##    value: "Europe/Paris"
##
extraEnvVars: []
## @param extraEnvVarsCM Name of existing ConfigMap containing extra env vars for FreeRADIUS containers
##
extraEnvVarsCM: ""
## @param extraEnvVarsSecret Name of existing Secret containing extra env vars for FreeRADIUS containers
##
extraEnvVarsSecret: ""

## Module enablement. Each sub-block toggles a matching FreeRADIUS module.
## Every enabled module renders into its own ConfigMap (`templates/modules/<name>.yaml`,
## named `<fullname>-mods-<name>`) and is mounted at
## `/etc/freeradius/mods-enabled/<name>` inside the container so the upstream
## daemon picks it up. Set `<module>.existingConfigMap` to mount a BYO ConfigMap
## (with a `<name>` key) instead — the chart then skips rendering that module.
##
modules:
  includeDir: mods-enabled
  ## SQL backend for clients/users/accounting. When enabled, the chart renders
  ## the `sql` module config (`templates/modules/sql.yaml`) directly from these
  ## values and the database-connection helpers (the matching bundled subchart —
  ## `mariadb.*` for mysql, `postgresql.*` for postgresql — or `externalDatabase.*`
  ## when no subchart is enabled), and mounts it into the pod. The DB password is
  ## injected separately by the Deployment via secretKeyRef.
  ##
  sql:
    ## @param modules.sql.enabled Enable the FreeRADIUS SQL module
    ##
    enabled: false
    ## @param modules.sql.existingConfigMap Mount an existing ConfigMap (must contain a `sql` key with the module config) at `mods-enabled/sql` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## @param modules.sql.dialect SQL dialect understood by the rlm_sql driver. Allowed: `mysql`, `postgresql`,
    ##  `sqlite`. Must match whichever bundled subchart is enabled (`mariadb.enabled` ↔ `mysql`, `postgresql.enabled` ↔ `postgresql`);
    ##  the chart's hard validator rejects mismatches.
    ##
    dialect: mysql
    ## @param modules.sql.table.acct1 Table containing accounting items (start records)
    ## @param modules.sql.table.acct2 Table containing accounting items (stop records)
    ## @param modules.sql.table.authcheck Table containing `check` items
    ## @param modules.sql.table.authreply Table containing `reply` items
    ## @param modules.sql.table.client Table holding RADIUS client/NAS info
    ## @param modules.sql.table.groupcheck Table containing group `check` items
    ## @param modules.sql.table.groupreply Table containing group `reply` items
    ## @param modules.sql.table.postauth Table for post-auth storage
    ## @param modules.sql.table.usergroup Table mapping users to groups
    ##
    table:
      acct1: radacct
      acct2: radacct
      authcheck: radcheck
      authreply: radreply
      client: nas
      groupcheck: radgroupcheck
      groupreply: radgroupreply
      postauth: radpostauth
      usergroup: radusergroup
    ## @param modules.sql.group_attribute The group attribute specific to this instance of rlm_sql (maps 1:1 to FreeRADIUS `group_attribute`)
    ##
    group_attribute: SQL-Group
    ## @param modules.sql.read_clients Read RADIUS clients from the database (`nas` table). Clients are only read on server startup. (Maps 1:1 to FreeRADIUS `read_clients`.)
    ##
    read_clients: true
    ## @param modules.sql.readGroups Read the group tables (`groupcheck`/`groupreply`) during authorization. When `true`, group tables are read unless `Fall-Through = no` is set in the reply table; when `false`, they are only read when `Fall-Through = yes` is set explicitly. Upstream rlm_sql default is `true`.
    ##
    readGroups: true
    ## @param modules.sql.readProfiles Read user profiles during authorization. When `true`, profiles are read unless `Fall-Through = no` is set in the groupreply table; when `false`, they are only read when `Fall-Through = yes` is set explicitly. Upstream rlm_sql default is `true`.
    ##
    readProfiles: true
    ## TLS configuration for the SQL connection itself. Independent of the
    ## in-pod RADSEC TLS (see top-level `tls:` block) and of cert-manager (see
    ## `tls.certManager.*`).
    ##
    ## @param modules.sql.tls.enabled Enable TLS for the SQL backend connection
    ## @param modules.sql.tls.ciphers TLS cipher list passed to the SQL driver (empty leaves the driver default)
    ## @param modules.sql.tls.autoGenerated [DEPRECATED — no longer consulted] When `modules.sql.tls.enabled: true` the chart now auto-generates a self-signed Secret implicitly (suppressed only by setting `modules.sql.tls.certificatesSecret`). Slated for removal in the next major bump.
    ## @param modules.sql.tls.certificatesSecret Name of an existing Secret holding `tls.crt` / `tls.key` / `ca.crt` for the SQL connection. Skips auto-generation when set.
    ## @param modules.sql.tls.certFilename Override filename of the leaf certificate inside the mounted Secret
    ## @param modules.sql.tls.certKeyFilename Override filename of the leaf key inside the mounted Secret
    ## @param modules.sql.tls.certCAFilename Override filename of the CA certificate inside the mounted Secret
    ## @param modules.sql.tls.existingTlsSecret [DEPRECATED — use `modules.sql.tls.certificatesSecret`] Legacy alias retained for backwards compatibility. Slated for removal in the next major bump.
    ##
    tls:
      enabled: false
      ciphers: ""
      autoGenerated: true
      certificatesSecret: ""
      certFilename: ""
      certKeyFilename: ""
      certCAFilename: ""
      existingTlsSecret: ""
    ## SQLite-specific knobs, used when `modules.sql.dialect: sqlite`.
    ## @param modules.sql.sqlite.filename Path inside the container where the SQLite database file lives
    ## @param modules.sql.sqlite.busyTimeout SQLite `busy_timeout` (milliseconds)
    ##
    sqlite:
      filename: /startechnica/freeradius/freeradius.db
      busyTimeout: "200"

  ## REST backend (rlm_rest). When enabled, the chart renders its own
  ## `<fullname>-mods-rest` ConfigMap (`templates/modules/rest.yaml`) and wires
  ## the values below into the module file via `$ENV{}` placeholders.
  ## Use the module to consult an HTTP/HTTPS API during the authorize /
  ## authenticate / accounting / post-auth virtual-server sections — each
  ## section only actually fires when the matching `sites-enabled/*` config
  ## lists `rest` in its block.
  ##
  ## Egress NetworkPolicy: when `networkPolicy.enabled: true` AND
  ## `networkPolicy.allowExternalEgress: false`, add an explicit rule under
  ## `networkPolicy.extraEgress` so the pod can reach `connect_uri`.
  ##
  rest:
    ## @param modules.rest.enabled Enable the FreeRADIUS REST module.
    ##
    enabled: false
    ## @param modules.rest.existingConfigMap Mount an existing ConfigMap (must contain a `rest` key with the module config) at `mods-enabled/rest` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## @param modules.rest.connect_uri Base URI all per-section URIs are joined against (e.g. `https://api.example.com/radius`). Required when `enabled: true`.
    ##
    connect_uri: ""
    ## @param modules.rest.connect_timeout TCP connect timeout in seconds (floats accepted).
    ##
    connect_timeout: "4.0"
    ## HTTP authentication applied to ALL per-section blocks (the same
    ## credentials are sent on every request). For per-section credentials,
    ## supply a custom `modules/rest` via `extraVolumes` /
    ## `extraVolumeMounts`.
    ##
    ## @param modules.rest.auth HTTP auth method. Allowed: `none`, `basic`, `digest`, `bearer`. When set to anything
    ##   other than `none`, the chart wires `FREERADIUS_MODS_REST_PASSWORD` from a Secret.
    ## @param modules.rest.username Username for `basic`/`digest` auth. For `bearer`, leave empty (the token is the password).
    ## @param modules.rest.password Password (or bearer token) sent on every request. Auto-generated to a chart-managed value when empty AND `auth != none` AND no `existingSecret` is set.
    ## @param modules.rest.existingSecret Name of an existing Secret holding the REST password. Overrides chart-managed Secret rendering.
    ## @param modules.rest.existingSecretPasswordKey Key inside `existingSecret` that holds the password (defaults to `mods-rest-password`).
    ##
    auth: none
    username: ""
    password: ""
    existingSecret: ""
    existingSecretPasswordKey: ""
    ## Per-section behavior. URI suffixes are appended to `connect_uri`; the
    ## method drives whether the request carries a body. Sections only fire
    ## when the matching virtual server (`sites-enabled/*`) calls `rest`.
    ##
    ## @param modules.rest.authorize.uri URI suffix appended to `connect_uri` for the authorize section. Empty means "use connect_uri as-is".
    ## @param modules.rest.authorize.method HTTP method. Allowed: `GET`, `POST`, `PUT`, `DELETE`, `PATCH`.
    ## @param modules.rest.authorize.body Body encoding. Allowed: `none`, `post` (form-urlencoded), `json`.
    ##
    authorize:
      uri: /authorize
      method: GET
      body: json
    ## @param modules.rest.authenticate.uri URI suffix for the authenticate section.
    ## @param modules.rest.authenticate.method HTTP method.
    ## @param modules.rest.authenticate.body Body encoding.
    ##
    authenticate:
      uri: /authenticate
      method: POST
      body: json
    ## @param modules.rest.accounting.uri URI suffix for the accounting section.
    ## @param modules.rest.accounting.method HTTP method.
    ## @param modules.rest.accounting.body Body encoding.
    ##
    accounting:
      uri: /accounting
      method: POST
      body: json
    ## @param modules.rest.post_auth.uri URI suffix for the post-auth section.
    ## @param modules.rest.post_auth.method HTTP method.
    ## @param modules.rest.post_auth.body Body encoding.
    ##
    post_auth:
      uri: /post-auth
      method: POST
      body: json
    ## TLS material for HTTPS `connect_uri`. Independent of in-pod RADSEC TLS
    ## (top-level `tls.*`) and SQL backend TLS (`modules.sql.tls.*`).
    ## Mounted into the pod at `/opt/startechnica/freeradius/certs-rest/`.
    ##
    ## Most REST endpoints only need server certificate verification — leave
    ## `enabled: false`, `check_cert: true`, `check_cert_cn: true` and the
    ## upstream image's system CA bundle verifies the server. Enable this
    ## block only when the endpoint signs with a private CA or requires
    ## mutual TLS (client cert).
    ##
    ## @param modules.rest.tls.enabled Mount client TLS material (CA / cert / key) into the pod and wire it into the rlm_rest `tls{}` block. Independent of `check_cert*` below.
    ## @param modules.rest.tls.auto_generated Render a chart-managed self-signed leaf via `templates/secrets/rest-tls.yaml` (signed by the chart's shared CA). Skipped when `certificates_secret` is set.
    ## @param modules.rest.tls.certificates_secret Name of an existing Secret holding `tls.crt`/`tls.key`/`ca.crt`. Overrides chart-managed rendering.
    ## @param modules.rest.tls.cert_filename Override filename of the leaf certificate inside the mounted Secret.
    ## @param modules.rest.tls.cert_key_filename Override filename of the leaf key inside the mounted Secret.
    ## @param modules.rest.tls.cert_ca_filename Override filename of the CA certificate inside the mounted Secret.
    ## @param modules.rest.tls.check_cert Verify the server's certificate chain. Disable only for self-signed endpoints in dev.
    ## @param modules.rest.tls.check_cert_cn Verify the server certificate's CN matches the URI host.
    ##
    tls:
      enabled: false
      auto_generated: false
      certificates_secret: ""
      cert_filename: ""
      cert_key_filename: ""
      cert_ca_filename: ""
      check_cert: true
      check_cert_cn: true
    ## rlm_rest connection pool (HTTP keepalive), sized off the FreeRADIUS
    ## thread pool. Every key is rendered into the `pool {}` block as a
    ## `key = value` directive via `freeradius.tplvalues.renderConfig`
    ## (`${thread[pool].*}` are FreeRADIUS config vars, passed through verbatim).
    ##
    ## @param modules.rest.pool.start Connections opened on module instantiation.
    ## @param modules.rest.pool.min Minimum idle connections kept open.
    ## @param modules.rest.pool.max Maximum open connections.
    ## @param modules.rest.pool.spare Spare connections kept idle.
    ## @param modules.rest.pool.uses Requests served per connection before recycle (0 = unlimited).
    ## @param modules.rest.pool.retry_delay Seconds to wait after a failed open before retrying.
    ## @param modules.rest.pool.lifetime Max connection lifetime in seconds (0 = unlimited).
    ## @param modules.rest.pool.idle_timeout Seconds an idle connection is kept before closing.
    ##
    pool:
      start: "${thread[pool].start_servers}"
      min: "${thread[pool].min_spare_servers}"
      max: "${thread[pool].max_servers}"
      spare: "${thread[pool].max_spare_servers}"
      uses: 0
      retry_delay: 30
      lifetime: 0
      idle_timeout: 60

  ## JSON xlat module (rlm_json). Exposes the `%{json_encode:...}` xlat for
  ## serialising attributes into JSON (handy for rlm_rest payloads and
  ## policies). On FreeRADIUS 3.2 the only knobs are the output format of that
  ## xlat, configured below under `encode`. These leaves are inlined into the
  ## nested `encode { attribute {} value {} }` block of `templates/modules/json.yaml`
  ## (the block structure is fixed; only these values are templated).
  ##
  json:
    ## @param modules.json.enabled Enable the FreeRADIUS JSON xlat module.
    ##
    enabled: false
    ## @param modules.json.existingConfigMap Mount an existing ConfigMap (must contain a `json` key with the module config) at `mods-enabled/json` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## Output format of the `%{json_encode:...}` xlat.
    ##
    encode:
      ## @param modules.json.encode.outputMode JSON document layout. One of: `object`, `object_simple`, `array`, `array_of_names`, `array_of_values`.
      ##
      output_mode: object
      attribute:
        ## @param modules.json.encode.attribute.prefix Colon-delimited prefix added to every attribute name (e.g. `foo` renders `User-Name` as `foo:User-Name`). Empty disables the prefix.
        ##
        prefix: ""
      value:
        ## @param modules.json.encode.value.singleValueAsArray Always emit values as a JSON array, even single values.
        ##
        singleValueAsArray: false
        ## @param modules.json.encode.value.enumAsInteger Emit the integer value of enumerated attributes instead of their textual alias.
        ##
        enumAsInteger: false
        ## @param modules.json.encode.value.datesAsInteger Emit date attributes as seconds since the epoch instead of a textual date.
        ##
        datesAsInteger: false
        ## @param modules.json.encode.value.alwaysString Force all values to be quoted JSON strings (integers are otherwise emitted unquoted).
        ##
        alwaysString: false

  ## PAM authentication module (rlm_pam). Hands authentication off to Linux
  ## PAM (`/etc/pam.d/${pam_auth}` inside the container). The upstream
  ## `freeradius/freeradius-server` image ships `/etc/pam.d/radiusd` by
  ## default — to use a custom PAM service, override `pam_auth` AND mount the
  ## matching config via `extraVolumes` / `extraVolumeMounts`. Don't forget
  ## to wire `Auth-Type := PAM` in `authorize` and add `pam` to the
  ## `authenticate {}` block of your virtual server.
  ##
  ## Every key here except `enabled` is rendered verbatim into the `pam {}`
  ## block as a `key = value` directive (via `freeradius.tplvalues.renderConfig`),
  ## so use FreeRADIUS directive names (snake_case) for any keys you add.
  ##
  pam:
    ## @param modules.pam.enabled Enable the FreeRADIUS PAM module.
    ##
    enabled: false
    ## @param modules.pam.existingConfigMap Mount an existing ConfigMap (must contain a `pam` key with the module config) at `mods-enabled/pam` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## @param modules.pam.pam_auth PAM service name. Maps to `/etc/pam.d/<name>` inside the container. The `Pam-Auth` attribute set in `authorize` overrides this at request time.
    ##
    pam_auth: radiusd

  ## PAP authentication module (rlm_pap). Verifies a cleartext `User-Password`
  ## against a "known good" password (`Cleartext-Password`, `Crypt-Password`,
  ## `MD5-Password`, `SSHA-Password`, …) supplied by another module such as
  ## `sql` or `files`. List `pap` in the `authorize {}` block of your virtual
  ## server (so it can set `Auth-Type := PAP`) and in `authenticate {}` (so it
  ## actually checks the password). This is the standalone module, independent
  ## of the inner `pap` EAP method under `modules.eap`.
  ##
  ## Every key here except `enabled`/`existingConfigMap` is rendered verbatim
  ## into the `pap {}` block as a `key = value` directive (via
  ## `freeradius.tplvalues.renderConfig`), so use FreeRADIUS directive names
  ## (snake_case) for any keys you add.
  ##
  pap:
    ## @param modules.pap.enabled Enable the FreeRADIUS PAP module.
    ##
    enabled: false
    ## @param modules.pap.existingConfigMap Mount an existing ConfigMap (must contain a `pap` key with the module config) at `mods-enabled/pap` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## @param modules.pap.normalise Detect the hash/encoding of "known good" passwords and normalise them so PAP can compare against them (`normalise`).
    ##
    normalise: true

  ## Redis module (rlm_redis). Exposes the `%{redis:...}` xlat for talking to a
  ## Redis server. Renders into `<fullname>-mods-redis`
  ## (`templates/modules/redis.yaml`) and mounts at `mods-enabled/redis`. Set
  ## `existingConfigMap` to mount a BYO ConfigMap instead. For a Redis-backed
  ## attribute cache, enable `modules.cache` with `driver: redis` — it reuses
  ## this connection (the settings below).
  ##
  ## Egress NetworkPolicy: when `networkPolicy.enabled: true` AND
  ## `networkPolicy.allowExternalEgress: false`, add a rule under
  ## `networkPolicy.extraEgress` so the pod can reach `modules.redis.server`.
  ##
  redis:
    ## @param modules.redis.enabled Enable the FreeRADIUS Redis module.
    ##
    enabled: false
    ## @param modules.redis.existingConfigMap Mount an existing ConfigMap (must contain a `redis` key) at `mods-enabled/redis` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## Redis connection. Every key here except `enabled` / `existingConfigMap` is
    ## rendered verbatim into the `redis { }` block as a `key = value` directive
    ## (nested maps become `key { }` sub-blocks) via
    ## `freeradius.tplvalues.renderConfig`, evaluated through Helm `tpl` (escape a
    ## literal `{{` as `{{ "{{" }}`). `server` is resolved by the chart (the bundled
    ## subchart's `<release>-redis-master` Service when `redis.enabled`, otherwise
    ## this value); the `${thread[pool].*}` values are FreeRADIUS config vars,
    ## passed through verbatim.
    ##
    ## @param modules.redis.server Redis server hostname. Ignored when the bundled `redis` subchart is enabled.
    ## @param modules.redis.port Redis server port.
    ## @param modules.redis.pool.start Connections opened on module instantiation.
    ## @param modules.redis.pool.min Minimum idle connections kept open.
    ## @param modules.redis.pool.max Maximum open connections.
    ## @param modules.redis.pool.spare Spare connections kept idle.
    ## @param modules.redis.pool.uses Requests served per connection before recycle (0 = unlimited).
    ## @param modules.redis.pool.retry_delay Seconds to wait after a failed open before retrying.
    ## @param modules.redis.pool.lifetime Max connection lifetime in seconds (0 = unlimited).
    ## @param modules.redis.pool.idle_timeout Seconds an idle connection is kept before closing.
    ##
    server: redis-master
    port: 6379
    # password: ""
    # database: 0
    pool:
      start: "${thread[pool].start_servers}"
      min: "${thread[pool].min_spare_servers}"
      max: "${thread[pool].max_servers}"
      spare: "${thread[pool].max_spare_servers}"
      uses: 0
      retry_delay: 30
      lifetime: 0
      idle_timeout: 60

  ## Cache module (rlm_cache). Caches arbitrary attributes keyed by an xlat
  ## expansion, with a pluggable backend `driver`. Renders into
  ## `<fullname>-mods-cache` (`templates/modules/cache.yaml`) and mounts at
  ## `mods-enabled/cache`. Set `existingConfigMap` to mount a BYO ConfigMap.
  ##
  cache:
    ## @param modules.cache.enabled Enable the FreeRADIUS cache module (rlm_cache).
    ##
    enabled: false
    ## @param modules.cache.existingConfigMap Mount an existing ConfigMap (must contain a `cache` key) at `mods-enabled/cache` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## @param modules.cache.driver Cache backend driver. One of `rbtree` (in-memory, per-pod), `redis` (shared across replicas — reuses the connection under `modules.redis`, and the bundled Redis password when `redis.auth.enabled`), `memcached`.
    ##
    driver: rbtree
    ## @param modules.cache.key Cache key expansion (FreeRADIUS xlat). Embed the quotes in the value so it renders quoted.
    ##
    key: '"%{User-Name}"'
    ## @param modules.cache.ttl Default entry time-to-live in seconds.
    ##
    ttl: 3600
    ## @param modules.cache.maxEntries Maximum number of cached entries (0 = unlimited).
    ##
    maxEntries: 0
    ## @param modules.cache.update Body of the required rlm_cache `update { }` section, rendered verbatim (through `tpl`, so values may be referenced). FreeRADIUS refuses to load the module unless this holds at least one map. Each line is FreeRADIUS map syntax `<list>:<attr> <op> <value>`: on store it is written to the cache entry, on retrieve it is copied back onto the request. The default caches the whole reply list; customise for what you actually need.
    ##
    update: |-
      &reply: += &reply:
    ## @param modules.cache.config [object] Extra `rlm_cache` directives merged verbatim into the `cache { }` block (e.g. `epoch`, `add_stats`). Rendered via `freeradius.tplvalues.renderConfig`.
    ##
    config: {}
    ## Memcached backend settings, used when `driver: memcached`. Rendered verbatim
    ## into the `memcached { }` sub-block.
    ##
    ## @param modules.cache.memcached.config [object] libmemcached driver settings (e.g. `options`).
    ##
    memcached:
      config:
        options: '"--SERVER=memcached:11211"'
    ## Additional user-defined rlm_cache instances rendered alongside the base
    ## `cache` instance (and the chart-managed `cache_tls` / `oidc_<name>_cache`
    ## siblings). Each top-level key is a FreeRADIUS module instance name
    ## (used directly in unlang as `<name>`) with the same schema as the base:
    ## `driver`, `key`, `ttl`, `maxEntries` (default 0), `update` (required),
    ## `config`, `memcached.config`. One ConfigMap per instance renders into
    ## `mods-enabled/<name>`. `driver: redis` auto-injects the `redis { }`
    ## sub-block reusing `modules.redis.*` connection params, same as the base.
    ##
    ## @param modules.cache.instances [object] Additional named rlm_cache instances. Map key = FreeRADIUS instance name (e.g. `cache_coa`); value = config dict mirroring the base cache schema.
    ## instances:
    ##   cache_coa:
    ##     driver: redis
    ##     key: '"%{Acct-Session-Id}"'
    ##     ttl: 7200
    ##     update: |-
    ##       &reply: += &reply:
    ##
    instances: {}

  ## EAP module (rlm_eap). Provides TLS-based EAP methods that share one
  ## `tls-config` server certificate: EAP-TLS (mutual TLS, client
  ## certificate) and EAP-TTLS (TLS tunnel + inner auth via a virtual
  ## server). `defaultType` selects which enabled method is offered to peers
  ## that don't negotiate one. EAP runs over plain RADIUS (UDP auth) — it is
  ## independent of RADSEC (`tls.enabled`).
  ##
  ## Structure mirrors FreeRADIUS: `tlsConfig` is the shared
  ## `tls-config tls-common { }` block (server cert + TLS engine settings);
  ## `methods.*` are the individual EAP methods, each referencing tls-common.
  ##
  eap:
    ## @param modules.eap.enabled Enable the FreeRADIUS EAP module.
    ##
    enabled: false
    ## @param modules.eap.existingConfigMap Mount an existing ConfigMap (must contain an `eap` key with the module config) at `mods-enabled/eap` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""
    ## @param modules.eap.defaultType Outer EAP method offered when the peer doesn't request a specific one. Must be one of the enabled outer methods (`tls`, `ttls`) listed in `methods`.
    ##
    defaultType: ttls
    ## @param modules.eap.timerExpire Seconds an EAP session may sit idle before it is expired (`timer_expire`).
    ## @param modules.eap.ignoreUnknownEapTypes Silently ignore EAP requests for unknown/unconfigured types instead of NAK-ing (`ignore_unknown_eap_types`).
    ## @param modules.eap.ciscoAccountingUsernameBug Work around the Cisco accounting User-Name bug (`cisco_accounting_username_bug`).
    ## @param modules.eap.maxSessions Maximum number of concurrent EAP sessions (`max_sessions`). Defaults to the FreeRADIUS `${max_requests}` config var.
    ##
    timerExpire: 60
    ignoreUnknownEapTypes: false
    ciscoAccountingUsernameBug: false
    maxSessions: "${max_requests}"
    ## @param modules.eap.methods [array] EAP methods to render, each as its own block. Outer methods (`tls`, `ttls`)
    ##   reference the shared `tlsConfig`; inner methods (`mschapv2`, `md5`, `pap`) render as empty blocks for use inside the TTLS tunnel (selected via `ttls.default_eap_type`). Remove a method to disable it — e.g. drop `md5` (EAP-MD5 is legacy/insecure).
    ##
    methods:
      - tls
      - ttls
      - peap
      - mschapv2
      - md5

    ## Per-method config. Which methods are rendered is controlled by `methods`
    ## above. Each method's config map (`modules.eap.<method>`) becomes the body
    ## of its block: every key is emitted as a `key = value` directive (nested
    ## maps become `key { ... }` sub-blocks) via `freeradius.tplvalues.renderConfig`,
    ## evaluated through Helm `tpl` so values may reference `.Values` / `.Release`
    ## (escape literal `{{` as `{{ "{{" }}`). The inner methods (`mschapv2` /
    ## `md5` / `pap`) carry no config here, so they render as empty `<method> {}`
    ## blocks; add any directive as a key to give one a body.
    ##
    ## @param modules.eap.tls.tls Name of the `tls-config` block this method references (`tls = <name>`).
    ##
    tls:
      tls: tls-common
    ## EAP-TTLS — establishes a TLS tunnel, then authenticates the inner
    ## identity through a virtual server.
    ##
    ## @param modules.eap.ttls.tls Name of the `tls-config` block this method references (`tls = <name>`).
    ## @param modules.eap.ttls.default_eap_type Inner EAP type used inside the TTLS tunnel (e.g. `mschapv2`, `md5`). Must be an inner method listed in `methods`.
    ## @param modules.eap.ttls.copy_request_to_tunnel Copy outer request attributes into the tunnel (`copy_request_to_tunnel`).
    ## @param modules.eap.ttls.use_tunneled_reply Copy tunnel reply attributes to the outer reply (`use_tunneled_reply`).
    ## @param modules.eap.ttls.virtual_server Virtual server (`sites-enabled/<name>`) that authenticates the TTLS inner identity. Defaults to the chart-shipped `inner-tunnel` site.
    ##
    ttls:
      tls: tls-common
      # default_eap_type: pap
      copy_request_to_tunnel: "yes"
      use_tunneled_reply: "yes"
      virtual_server: inner-tunnel
      include_length: "yes"

    peap:
      tls: tls-common
      # default_eap_type: pap
      copy_request_to_tunnel: "yes"
      use_tunneled_reply: "yes"
      virtual_server: "inner-tunnel"

    ## Shared TLS server configuration — FreeRADIUS `tls-config tls-common`.
    ## Consumed by every enabled TLS-based EAP method above. A separate TLS
    ## context (independent of in-pod RADSEC `tls.*`, SQL `modules.sql.tls.*`,
    ## and REST `modules.rest.tls.*`), mounted at
    ## `/opt/startechnica/freeradius/certs-eap`. EAP cannot run without a
    ## server certificate, so the chart auto-generates one (via cert-manager
    ## when its API is present, otherwise via the in-template shared genCA
    ## path) whenever the module is enabled and `certificates_secret` is
    ## empty.
    ##
    ## @param modules.eap.tlsConfig.name Name of the `tls-config` block (FreeRADIUS `tls-config <name>`). Each enabled method references it via `tls = <name>`.
    ## @param modules.eap.tlsConfig.autoGenerated [DEPRECATED — no longer consulted] When `modules.eap.enabled: true` the chart now auto-generates the EAP server leaf implicitly (suppressed only by setting `modules.eap.tlsConfig.certificates_secret`). Slated for removal in the next major bump.
    ## @param modules.eap.tlsConfig.certificates_secret Name of an existing Secret holding `tls.crt`/`tls.key`/`ca.crt` for EAP. Overrides chart-managed rendering.
    ## @param modules.eap.tlsConfig.cert_filename Override filename of the leaf certificate inside the mounted Secret.
    ## @param modules.eap.tlsConfig.cert_key_filename Override filename of the leaf key inside the mounted Secret.
    ## @param modules.eap.tlsConfig.cert_ca_filename Override filename of the CA certificate inside the mounted Secret.
    ## @param modules.eap.tlsConfig.private_key_password Password for the EAP leaf key when it is password-protected. Auto-generated into the chart credentials Secret (`mods-eap-tls-privkey-password`) when empty.
    ## @param modules.eap.tlsConfig.random_file Source of randomness passed to the TLS engine (`random_file`).
    ## @param modules.eap.tlsConfig.cipher_list OpenSSL cipher list (`cipher_list`). `DEFAULT` keeps the image's compiled-in list.
    ## @param modules.eap.tlsConfig.cipher_server_preference Honour the server's cipher order over the client's (`cipher_server_preference`).
    ## @param modules.eap.tlsConfig.min_version Minimum TLS protocol version offered (`tls_min_version`).
    ## @param modules.eap.tlsConfig.max_version Maximum TLS protocol version offered (`tls_max_version`).
    ##
    tlsConfig:
      name: tls-common
      autoGenerated: true
      certificates_secret: ""
      cert_filename: ""
      cert_key_filename: ""
      cert_ca_filename: ""
      private_key_password: ""
      random_file: /dev/urandom
      cipher_list: "DEFAULT"
      cipher_server_preference: false
      min_version: "1.2"
      max_version: "1.3"
      ## @param modules.eap.tlsConfig.cache.enable Enable the EAP TLS session cache (`cache { enable }`). Use `"yes"`/`"no"` (quoted, rendered verbatim). Overridden to `yes` when `sites.tlsCache.enabled` (the chart auto-wires that path).
      ## EAP TLS session cache — OpenSSL session resumption for EAP-TLS / PEAP /
      ## TTLS (distinct from `rlm_cache` / `modules.cache`). Enabling lets a
      ## returning supplicant skip the full handshake.
      ##
      ## Multi-replica caveat: the cache is per-pod (in-memory by default).
      ## `persist_dir` writes local disk only — shared RWX storage is unsafe for
      ## concurrent OpenSSL session writes. With multiple replicas a supplicant
      ## resuming on a different pod than the original will fall back to a full
      ## handshake; useful only with `replicas: 1` or sticky session affinity on
      ## the Service. For cross-pod resumption, enable `sites.tlsCache` — it
      ## externalises the cache to a Redis-backed rlm_cache instance and
      ## auto-wires `method = stateful` / `virtual_server = tls-cache` here.
      ##
      cache:
        enable: "no"
        lifetime: 86400                          # session lifetime in seconds (24h)
        max_entries: 255                         # in-memory entries cap
        name: EAP-module                         # namespace for the cache (lets multiple tls-configs coexist)
        persist_dir: "${logdir}/tlscache"        # OPTIONAL — persist sessions to disk so resumption survives restarts
        store_attributes: "yes"                  # write session attrs back into the request on resume
      ## @param modules.eap.tlsConfig.ocsp.enable Enable OCSP certificate checking (`ocsp { enable }`). Use `"yes"`/`"no"` (quoted, rendered verbatim).
      ##
      ocsp:
        enable: "no"
        name: EAP-module
        persist_dir: "${logdir}/tlscache"
      verify: {}
        # tmpdir: /tmp/radiusd
        # client: "/usr/bin/openssl verify -CApath ${..ca_path} %{TLS-Client-Cert-Filename}"
  files:
    enabled: false
    existingConfigMap: ""
  ldap:
    ## @param modules.ldap.enabled Enable the FreeRADIUS LDAP module.
    ##
    enabled: false
    ## @param modules.ldap.existingConfigMap Mount an existing ConfigMap (must contain an `ldap` key with the module config) at `mods-enabled/ldap` instead of the chart-rendered one; the chart then skips rendering its own.
    ##
    existingConfigMap: ""

  ## Generic OIDC / OAuth 2.0 authentication backend. RFC 6749 password
  ## grant (ROPC) plus optional RFC 7662 introspection. Works against any
  ## OIDC provider that exposes a token endpoint (Authentik, Auth0,
  ## Azure AD, Okta, generic Keycloak, …) — the per-instance wrapper
  ## bakes the resolved endpoints into the Python module at chart-render
  ## time, so the runtime library has no provider-specific code.
  ##
  ## Multi-instance: dispatch via `clients.<x>.oidc: <instance-name>`,
  ## optional rlm_cache, optional refresh-token freshness check. The
  ## per-instance Python wrapper is generated by Helm with all OIDC
  ## endpoints + claim paths baked in at chart-render time.
  ##
  ## @param modules.oidc.enabled Render the OIDC auth configs + Secrets and mount them into the pod. Acts as the master feature gate; renders nothing when `false`.
  ## @param modules.oidc.wireDefaultSite Auto-wire the `default` instance's `oidc_authorize` as the `else` branch of the dispatch chain in `sites/default` (catches NAS that aren't bound to a named instance via `clients.<x>.oidc`). No-op when no `default` instance is configured.
  ## @param modules.oidc.unmatchedReject When `true` AND no `default` instance is wired, the dispatch chain's `else` branch rejects unmatched NAS instead of falling through to `pap`. Off by default.
  ## @param modules.oidc.instances [object] Named OIDC backends. Map key = FreeRADIUS module instance name (matching `^[a-z][a-z0-9_]*$`); value = backend config dict. Bind a NAS to one with `clients.<x>.oidc: <instance-name>`.
  ##
  ## Per-instance keys (rendered into the Python wrapper at chart-render time):
  ##   tokenUrl              str — RFC 6749 token endpoint (REQUIRED)
  ##   introspectUrl         str — RFC 7662 introspect endpoint (REQUIRED when `introspect: true`)
  ##   clientId              str — OAuth client_id
  ##   clientSecret          str — confidential-client secret (Secret-backed; or use `existingSecret`)
  ##   existingSecret        str — BYO Secret holding key `client-secret`
  ##   scope                 str — space-separated OAuth scope (e.g. "openid profile email")
  ##   connectTimeout        str — connect timeout in seconds
  ##   roleAttribute         str — control attribute populated per role (default `Class`)
  ##   rolesClaim            str — dotted path into JWT/introspect claims that yields a role list
  ##                                (e.g. `roles`, `realm_access.roles`, `resource_access.<id>.roles`)
  ##   denyWithoutRole       bool — reject when no role matches `roleMappings`
  ##   roleMappings          list — ordered `[{role, reply: [...]}]` (first match wins)
  ##   groupAttribute        str — control attribute populated per group (default `Class`)
  ##   groupsClaim           str — dotted path that yields a group list (default `groups`)
  ##   groupMappings         list — ordered `[{group, reply: [...]}]` (first match wins)
  ##   attributeMappings     list — generic `[{claim, reply}]` claim→reply copy
  ##   require               list — JWT claim names that must be truthy (e.g. `[email_verified]`)
  ##   introspect            bool — use RFC 7662 introspect over local JWT decode (catches revocation)
  ##   refreshTokenCache     bool — cache refresh_token + run a freshness check on cache hit
  ##   cache.enabled         bool — render a `oidc_<name>_cache` rlm_cache instance
  ##   cache.ttl             int — cache TTL in seconds
  ##   tls.caCert            str — inline PEM CA bundle (for verifying the IdP cert)
  ##   tls.existingSecret    str — BYO Secret containing the CA bundle
  ##   tls.existingSecretCaKey str — key within `existingSecret` holding the bundle
  ##   tls.insecure          bool — skip TLS verification (dev only)
  ##   existingConfigMap     str — BYO module ConfigMap (skips chart-rendered one)
  ##
  ## instances:
  ##   default:
  ##     tokenUrl: "https://idp.example.com/oauth/token"
  ##     clientId: freeradius
  ##     clientSecret: ""
  ##   authentik:
  ##     tokenUrl: "https://auth.example.com/application/o/freeradius/token/"
  ##     introspectUrl: "https://auth.example.com/application/o/introspect/"
  ##     clientId: freeradius
  ##     clientSecret: "..."
  ##     scope: "openid profile email"
  ##     rolesClaim: groups       # Authentik puts group membership in `groups`
  ##     require: [email_verified]
  ##     introspect: true
  ##     cache:
  ##       enabled: true
  ##       ttl: 300
  ##     roleMappings:
  ##       - role: network-admin
  ##         reply: ['Service-Type := Administrative-User']
  ##
  oidc:
    enabled: false
    wireDefaultSite: true
    unmatchedReject: false
    instances: {}

## FreeRADIUS policy definitions (the `policy { }` block in radiusd.conf).
##
## @param policies.includeDir Directory the `policy { }` block includes policies from. A relative path resolves against the config dir (`/etc/freeradius`), so the default reads the image's bundled `policy.d` (filter, eap, …) plus any chart-mounted policies (e.g. OIDC's `policy.d/oidc`). The directory must exist in the container — point it at an absolute path only if you mount your own populated `policy.d`.
##
policies:
  includeDir: policy.d

## FreeRADIUS home_server definitions for proxy routing. Each entry renders a
## `home_server <name> { ... }` block referenced by a `home_server_pool`.
## Rendered into the `<fullname>-home-servers` ConfigMap, mounted at
## `/opt/startechnica/freeradius/home-servers.conf` and `$INCLUDE`d from the
## main configuration (absolute path) when this array is non-empty.
## `homeServerPools` and `realms` render into their own sibling ConfigMaps
## (`-home-server-pools` / `-realms`) under the same mount root and are
## `$INCLUDE`d in dependency order (servers → pools → realms).
##
## @param homeServers [array] Inline home_server entries. Each entry: `name` is the home_server identifier (referenced by `homeServerPools[].homeServers`); `type` is `auth` / `acct` / `auth+acct` / `coa` (default `auth`); `ipaddr` / `port` address the remote (port default 1812); `secret` is the shared secret (use `$ENV{...}` to inject from the env, e.g. via `extraEnvVarsSecret`); `proto` is `udp` / `tcp` (default `udp`); the remaining fields (`response_window`, `zombie_period`, `revive_interval`, `status_check`, `check_interval`, `num_answers_to_alive`) map 1:1 to FreeRADIUS native `home_server { }` directives and are emitted only when set.
## homeServers:
##   - name: auth1
##     type: auth
##     ipaddr: 192.0.2.10
##     port: 1812
##     secret: $ENV{FREERADIUS_HOMESERVER_AUTH1_SECRET}
##     proto: udp
##     response_window: 20
##     zombie_period: 40
##     revive_interval: 120
##     status_check: status-server
##     check_interval: 30
##     num_answers_to_alive: 3
##
homeServers: []

## FreeRADIUS home_server_pool definitions. Each entry renders a
## `home_server_pool <name> { ... }` block listing one or more `home_server`
## members; realms reference pools via `realms[].authPool` / `acctPool`.
##
## @param homeServerPools [array] Inline home_server_pool entries. Each entry: `name` is the pool identifier referenced by realms; `type` is `fail-over` (default) / `load-balance` / `client-balance` / `client-port-balance` / `keyed-balance`; `home_servers` is the ordered list of `home_server` names belonging to this pool (each renders as one `home_server = <name>` line); `virtual_server` (optional, maps 1:1 to the FreeRADIUS `virtual_server` directive in `home_server_pool { }`) names a virtual server the proxied reply is processed through; `fallback` (optional) names a `home_server` used when every pool member is dead. Note: per-entry sub-fields use snake_case here for visual consistency with the rendered FreeRADIUS config; this is distinct from the top-level `.Values.homeServers` (camelCase, list of home_server *definitions*).
## homeServerPools:
##   - name: my_auth_failover
##     type: fail-over
##     home_servers:
##       - auth1
##       - auth2
##     virtual_server: ""
##     fallback: ""
##
homeServerPools: []

## FreeRADIUS realm definitions for proxy routing. Each entry renders a
## `realm <name> { ... }` block. Realms map an incoming request's User-Name
## suffix to a `home_server_pool` (`authPool` / `acctPool`) or hand the
## request to a local `virtualServer`. Leave empty to keep the upstream
## `proxy.conf` realms unchanged.
##
## @param realms [array] Inline realm entries. Each entry: `name` is the realm string matched against the User-Name suffix (use `DEFAULT` for the fall-through realm, `NULL` for requests with no realm, `LOCAL` for the upstream-convention "handle locally, do not proxy" sentinel). All optional sub-fields below use snake_case mirroring FreeRADIUS native `realm { }` directive names; emit only the ones you need (mixing old-style and new-style is rejected by FreeRADIUS). Two routing modes: (a) **new-style proxy** via `auth_pool` / `acct_pool` / `coa_pool`, each referencing a `home_server_pool` name from `homeServerPools[]` (auth = Access-Request, acct = Accounting-Request, coa = Change-of-Authorization / Disconnect-Request); (b) **old-style proxy** via `authhost` / `accthost` / `secret` (FreeRADIUS 1.x/2.x compatibility — host:port pairs and the shared secret in-line). `virtual_server` handles the request locally instead of proxying (alternative to both modes). `nostrip: true` keeps the realm portion of User-Name when proxying (default strips). `realm LOCAL` is emitted by the chart unconditionally (the upstream definition in `files/proxy.conf` is commented out to avoid a "Duplicate realm" error). Supplying your own `name: LOCAL` entry below overrides the chart-managed one — useful for pointing LOCAL at a custom `virtual_server`. Wrapping charts can override `realms` freely without losing LOCAL.
## realms:
##   - name: example.com                  # new-style proxy via pool refs
##     auth_pool: my_auth_failover
##     acct_pool: my_acct_failover
##     coa_pool: my_coa_pool
##     nostrip: false
##     virtual_server: ""
##   - name: legacy.example.com           # old-style proxy
##     authhost: radius.example.com:1600
##     accthost: radius.example.com:1601
##     secret: $ENV{FREERADIUS_REALM_LEGACY_SECRET}
##
realms: []

## Chart-managed default proxy primitives — three independent toggles, each
## emits one config piece. The chart does NOT cross-validate them: enabling
## `realm` without `homeServerPool` emits a `realm DEFAULT` that references
## pools you must supply via `homeServerPools[]` (else FreeRADIUS fails at
## startup with "Reference to non-existent pool"); enabling `homeServerPool`
## without `homeServer` similarly references home_servers you supply via
## `homeServers[]`. Loud-fail-at-startup is the desired feedback.
##
## All three are no-op when `kind != StatefulSet` — the per-pod `home_server`
## entries depend on stable per-pod DNS via the headless Service, which only
## StatefulSet provides; the realm and pools dangle without their underlying
## home_servers, so the whole feature is gated together.
##
## Identifiers derive from `<fullname>` snake-cased (hyphens → underscores
## via `freeradius.utils.snakeCase`). For a release `demo-freeradius` with
## `replicaCount: 2`, all three toggles on:
##
##   realm DEFAULT {
##       auth_pool = demo_freeradius_auth_pool
##       acct_pool = demo_freeradius_acct_pool
##   }
##   home_server demo_freeradius_0_auth { type = auth; ipaddr = "demo-freeradius-0.demo-freeradius-headless.<ns>.svc.cluster.local"; ... }
##   home_server demo_freeradius_0_acct { type = acct; ... }
##   home_server demo_freeradius_1_auth { ... }
##   home_server demo_freeradius_1_acct { ... }
##   home_server_pool demo_freeradius_auth_pool { type = load-balance; home_server = demo_freeradius_0_auth; home_server = demo_freeradius_1_auth }
##   home_server_pool demo_freeradius_acct_pool { type = load-balance; home_server = demo_freeradius_0_acct; home_server = demo_freeradius_1_acct }
##
## The home_server `secret` renders literally as
## `secret = $ENV{FREERADIUS_DEFAULT_INSTANCE_SECRET}` — wire the env var via
## `extraEnvVarsSecret` and use the same `$ENV{...}` expansion in the
## matching `clients{}` entry so peer-pod Access-Request packets authenticate.
##
## @param createDefaultInstance.realm Emit a `realm DEFAULT { auth_pool, acct_pool }` block referencing the auto-gen pools.
## @param createDefaultInstance.homeServer Emit one `home_server <fullname>_<ord>_auth` and one `<fullname>_<ord>_acct` per StatefulSet replica.
## @param createDefaultInstance.homeServerPool Emit one `home_server_pool <fullname>_auth_pool` and one `<fullname>_acct_pool` listing the per-pod home_servers.
##
createDefaultInstance:
  realm: false
  homeServer: false
  homeServerPool: false

## Virtual-server enablement. Toggles the matching FreeRADIUS virtual server
## (`/etc/freeradius/sites-enabled/<name>` inside the container) and wires
## the corresponding container port / Service port / NetworkPolicy entry.
## Per-site config files are sourced from `files/sites/<name>` and rendered
## into the `<release>-sites` ConfigMap by `templates/configmaps/sites.yaml`.
##
sites:
  includeDir: /opt/startechnica/freeradius/sites-enabled
  ## Core virtual servers, enabled by default: `default` (the main auth/acct
  ## server) and `inner-tunnel` (EAP-TTLS/PEAP inner identity). Disable either
  ## with `enabled: false`, or set `existingConfigMap` to bring your own.
  ##
  ## @param sites.default.enabled Enable the `default` virtual server (the main auth/acct server). Leave enabled unless you fully replace it via `extraSites`.
  ## @param sites.default.existingConfigMap Mount an existing ConfigMap (must contain a `default` key with the virtual-server config) at `sites-enabled/default` instead of the chart-rendered one; the chart then skips rendering its own.
  ##
  default:
    enabled: true
    existingConfigMap: ""
  ## @param sites.innerTunnel.enabled Enable the `inner-tunnel` virtual server (EAP-TTLS/PEAP inner identity). Required by the EAP module's tunnelled methods.
  ## @param sites.innerTunnel.existingConfigMap Mount an existing ConfigMap (must contain an `inner-tunnel` key with the virtual-server config) at `sites-enabled/inner-tunnel` instead of the chart-rendered one; the chart then skips rendering its own.
  ##
  innerTunnel:
    enabled: true
    existingConfigMap: ""
  ## @param sites.coa.enabled Enable the `coa` virtual server (Change-of-Authorization, UDP port `containerPorts.coa`).
  ## @param sites.coa.existingConfigMap Mount an existing ConfigMap (must contain a `coa` key with the virtual-server config) at `sites-enabled/coa` instead of the chart-rendered one; the chart then skips rendering its own.
  ## @param sites.coa.listen.ipaddr Bind address for the CoA listener. Wildcard `*` accepts CoA traffic on every pod interface; set to `0.0.0.0` / a specific IPv4 / `::` for IPv6 to constrain.
  ##
  coa:
    enabled: false
    existingConfigMap: ""
    listen:
      ipaddr: "*"
  ## RADIUS `status` virtual server, used by the chart's liveness/readiness
  ## probes (`radclient status` over loopback) AND by the standalone metrics
  ## exporter Deployment (`metrics.enabled`) which talks to it over the
  ## cluster Service. Disabling this also disables the chart-managed probes
  ## — supply `customLivenessProbe`/`customReadinessProbe` if you need
  ## different checks.
  ##
  ## @param sites.status.enabled Enable the `status` virtual server
  ## @param sites.status.listen Listen address for the status server. Default `0.0.0.0` so the standalone metrics exporter pod can reach it via the FreeRADIUS Service. Use `127.0.0.1` to keep it pod-local if you don't enable metrics (the chart's NetworkPolicy locks down the port either way).
  ## @param sites.status.secret Shared secret the status `radclient` uses. When empty, the chart auto-generates and persists one in `auth.existingSecret` under key `sites-status-secret`.
  ## @param sites.status.existingConfigMap Mount an existing ConfigMap (must contain a `status` key with the virtual-server config) at `sites-enabled/status` instead of the chart-rendered one; the chart then skips rendering its own.
  ##
  status:
    enabled: true
    existingConfigMap: ""
    listen: 0.0.0.0
    secret: ""
  ## DHCP virtual server (rlm_dhcp / sqlippool). Disabled by default. Enabling
  ## mounts the `dhcp` virtual server but does not open a DHCP listener port —
  ## wire that up via `containerPorts` / `service` / `extraPorts` as needed.
  ##
  ## @param sites.dhcp.enabled Enable the `dhcp` virtual server
  ## @param sites.dhcp.existingConfigMap Mount an existing ConfigMap (must contain a `dhcp` key with the virtual-server config) at `sites-enabled/dhcp` instead of the chart-rendered one; the chart then skips rendering its own.
  ##
  dhcp:
    enabled: false
    existingConfigMap: ""
  ## In-pod RADSEC server (TLS over TCP RADIUS). When enabled, the chart
  ## mounts the TLS material referenced by `tls.*` and opens the RADSEC
  ## container/Service port.
  ##
  ## @param sites.radsec.enabled Enable the RADSEC virtual server
  ## @param sites.radsec.listen.ipaddr Bind address for the RADSEC `listen { }` block. `*` accepts on every pod interface; set to `0.0.0.0` / a specific IPv4 / `::` for IPv6 to constrain. Note: the port is *not* configurable here — it is shared with the Service and NetworkPolicy via `containerPorts.radsec`.
  ## @param sites.radsec.listen.type FreeRADIUS listen-socket type. Default `auth+acct` handles both Access-Request and Accounting-Request packets on the same RADSEC connection — typical for a RADSEC-replacement-for-UDP setup. Use `auth` or `acct` to constrain.
  ## @param sites.radsec.listen.virtual_server Virtual server that processes decrypted RADSEC requests. Default `default` routes to the same `server default { }` that handles UDP traffic — see [README.md](README.md) §RADSEC for the rationale. Override to send RADSEC requests through a separate processing pipeline (must reference an existing virtual server, e.g. via `extraSites` or `sites.<name>.enabled`).
  ## @param sites.radsec.listen.proxy_protocol Emit `proxy_protocol = yes` so the listener parses HAProxy PROXY-protocol headers and uses the real client IP for RADIUS client lookup. Useful when fronting the RADSEC port with an L4 load balancer that adds PROXY headers. Default `false` (directive omitted, matches FreeRADIUS image default).
  ## @param sites.radsec.listen.check_client_connections Emit `check_client_connections = yes` so FreeRADIUS validates the client connection (TCP/TLS handshake) up front and rejects unauthorised peers before parsing RADIUS packets. Default `false` (directive omitted, matches FreeRADIUS image default).
  ## @param sites.radsec.tls.cipher_list [array] OpenSSL cipher tokens passed to the RADSEC server's `tls { cipher_list = ... }` directive. The chart joins the entries with `:` (OpenSSL cipher-list separator) and applies the same value to the chart-managed `home_server radsec` loopback block in `home-servers.conf` so both peers negotiate consistently. The single-item list `["DEFAULT"]` keeps the upstream image's default list; set to explicit tokens to constrain (e.g. `["HIGH", "!aNULL", "!MD5"]` → `HIGH:!aNULL:!MD5`). Empty list `[]` also falls back to `DEFAULT`.
  ## @param sites.radsec.tls.private_key_password Password for the RADSEC private key when it is password-protected. Renders the `tls { private_key_password = $ENV{...} }` line in both the radsec listener and the chart-managed `home_server radsec` loopback. Auto-generated into the chart credentials Secret (`sites-radsec-privkey-password`) when left empty; the env-var (`FREERADIUS_SITES_RADSEC_PRIVKEY_PASSWORD`) is wired only when this value is set.
  ## @param sites.radsec.radsecSecret RADIUS shared secret for the loopback `clients radsec { client 127.0.0.1 { secret = … } }` block in the RADSEC virtual server. Stored under `clients-radsec-secret` in the chart-managed credentials Secret and injected via `$ENV{FREERADIUS_CLIENTS_RADSEC_SECRET}`. Auto-generated when empty; FreeRADIUS defaults the secret to literal `"radsec"` for `proto = tls` so any value works.
  ## @param sites.radsec.existingConfigMap Mount an existing ConfigMap (must contain a `radsec` key with the virtual-server config) at `sites-enabled/radsec` instead of the chart-rendered one; the chart then skips rendering its own.
  ## @param sites.radsec.clients [array] Additional external RADSEC peers (NASes, WiFi controllers, switches, VPN concentrators) authorised to connect on port `containerPorts.radsec`. Each entry renders a `client <name> { ... }` block inside the existing `clients radsec { }` group; the chart-managed loopback `client 127.0.0.1` (used by `home_server radsec` for the self-proxy pattern) is always emitted alongside, so don't add `127.0.0.1` here. Each entry: `name` is the on-config identifier (DNS-1123 label); `ipaddr` is an IPv4 host or CIDR (`192.0.2.50` / `192.0.2.0/24`); `ipv6addr` optional IPv6 variant — setting BOTH on the same entry renders TWO sibling `client { }` blocks (`<name>` for v4, `<name>_v6` for v6) sharing every other directive, since FreeRADIUS client blocks hold a single address each; `secret` is the RADIUS shared secret — FreeRADIUS defaults this to literal `"radsec"` for `proto = tls`, so an empty value works for vanilla RADSEC, set it when your NAS uses a real secret (use `$ENV{...}` to pull from `extraEnvVarsSecret`); `require_message_authenticator: true` enforces the BlastRADIUS mitigation per client; `nas_type` is a free-form NAS-Type tag (default `other`); `virtual_server` (optional) overrides the listener's default virtual server for this client. All field names mirror FreeRADIUS native `client { }` directive names.
  ##
  radsec:
    enabled: false
    existingConfigMap: ""
    radsecSecret: ""
    listen:
      ipaddr: "*"
      type: "auth+acct"
      virtual_server: "default"
      proxy_protocol: false
      check_client_connections: false
    tls:
      cipher_list:
        - "DEFAULT"
      private_key_password: ""
    ## clients:
    ##   - name: ap-controller
    ##     ipaddr: 192.0.2.50/32
    ##     secret: $ENV{FREERADIUS_RADSEC_CLIENT_AP_SECRET}
    ##     require_message_authenticator: true
    ##     nas_type: cisco
    ##     virtual_server: ""
    ##     ipv6addr: ""
    ##
    clients: []
  ## `tls-cache` virtual server — backs EAP TLS session resumption with a
  ## shared Redis-backed rlm_cache instance, fixing the per-pod caveat
  ## documented on `modules.eap.tlsConfig.cache` above. Enabling renders:
  ##   * `sites-enabled/tls-cache` (this template)
  ##   * `mods-enabled/cache_tls` — dedicated rlm_cache instance keyed on
  ##     `%{TLS-Session-Id}`, reusing the Redis connection from `modules.redis.*`
  ##   * auto-wires `tls-config { cache { enable = yes, method = stateful,
  ##     virtual_server = tls-cache } }` into the EAP module
  ## REQUIRES `modules.eap.enabled: true` AND a Redis backend (bundled subchart
  ## `redis.enabled: true` or external `modules.redis.server`). Rendering
  ## hard-fails via `freeradius.validate.tlsCache` otherwise — an in-pod
  ## backend would defeat the cross-replica resumption this site exists for.
  ##
  ## @param sites.tlsCache.enabled Enable the `tls-cache` virtual server. Auto-wires the EAP cache directives; hard-fails without Redis.
  ## @param sites.tlsCache.existingConfigMap Mount an existing ConfigMap (must contain a `tls-cache` key with the virtual-server config) at `sites-enabled/tls-cache` instead of the chart-rendered one; the chart then skips rendering its own.
  ## @param sites.tlsCache.ttl TTL (seconds) for cached TLS sessions in the dedicated `cache_tls` instance. Should match the desired resumption window (typically aligned with `modules.eap.tlsConfig.cache.lifetime` if set).
  ##
  tlsCache:
    enabled: false
    existingConfigMap: ""
    ttl: 86400


## RADIUS clients map rendered into a ConfigMap and mounted at
## `/etc/freeradius/clients.conf`. Each top-level entry becomes one `client`
## block. Set `existingConfigMapName` to bring your own ConfigMap (the entries
## below are then ignored).
##
clients:
  includeFile: clients.conf
  ## @param clients.existingConfigMapName Name of an existing ConfigMap supplying `clients.conf`. Overrides the chart-rendered entries above.
  ##
  existingConfigMapName: ""
  ## @param clients.localhost.ipv4addr IPv4 address (or CIDR) the loopback client matches. Empty skips IPv4 (set `ipv6addr` for a v6-only NAS).
  ## @param clients.localhost.ipv6addr IPv6 address (or CIDR) the loopback client matches. Setting BOTH `ipv4addr` and `ipv6addr` on the same entry renders TWO sibling `client { }` blocks (`<name>` for v4, `<name>_v6` for v6) sharing every other directive — FreeRADIUS client blocks hold a single address each, so the chart splits the entry to express "one logical NAS with two addresses".
  ## @param clients.localhost.proto Transport protocol (`udp` or `tcp`)
  ## @param clients.localhost.secret Shared secret used to authenticate this client
  ## @param clients.localhost.nas_type NAS-Type string reported by RADIUS attributes
  ## @param clients.localhost.virtual_server Virtual server (`sites-enabled/<name>`) requests from this client are dispatched to
  ## @param clients.localhost.coa_server Name of a `home_server` (or `home_server_pool`) used to proxy **outbound** CoA-Request/Disconnect packets sent to this client. Leave empty unless you've defined a matching `home_server` block — this chart does NOT currently render any. Inbound CoA is handled separately by `sites.coa.enabled` (a `listen` block + virtual server, not a client directive).
  ## @param clients.localhost.require_message_authenticator Reject Access-Requests that arrive without a `Message-Authenticator` attribute. Mitigates the BlastRADIUS attack (CVE-2024-3596). Safe to enable when every NAS that hits this client entry is known to add Message-Authenticator (all modern radclients do).
  ## @param clients.localhost.oidc Bind this NAS to a named OIDC backend defined under `modules.oidc.instances.<name>`. When set, the chart renders an `if (Packet-Src-IP-Address == <ipv4addr>) { oidc_<name>_authorize }` arm into the dispatch chain in `sites/default`. Leave empty to fall through to the `default` instance (when `modules.oidc.wireDefaultSite: true`) or to `pap`. Not emitted into `clients.conf` — FR3 client blocks are schema-validated and reject unknown directives.
  ## @param clients.localhost.limit.max_connections Maximum concurrent connections (TCP only)
  ## @param clients.localhost.limit.lifetime Maximum connection lifetime in seconds (0 = no limit)
  ## @param clients.localhost.limit.idle_timeout Idle timeout in seconds
  ##
  localhost:
    ipv4addr: "127.0.0.1"
    ipv6addr: ""
    proto: udp
    secret: password
    nas_type: other
    virtual_server: default
    coa_server: ""
    require_message_authenticator: false
    oidc: ""
    limit:
      max_connections: 16
      lifetime: 0
      idle_timeout: 30


## RADIUS users for the `files` module. When non-empty, the chart renders
## `/etc/freeradius/mods-config/files/authorize` from these entries
## (`templates/configmaps/users.yaml`) and mounts it via `subPath` so the
## upstream image's existing `mods-enabled/files` symlink picks it up
## automatically (no need to set `modules.files.enabled`). The `default`
## virtual server already calls `files` unconditionally in `authorize {}`.
##
## File-based user storage is fine for small / static / dev deployments;
## for production multi-user setups with mutations through the day, switch to
## `modules.sql.enabled: true` (durable, queryable, no pod restart on change).
##
## Password hygiene note: values supplied here are baked into a ConfigMap
## (not a Secret) and visible to anyone with read access to the namespace.
## For dev/test that's usually fine; for anything sensitive, use the SQL
## backend with a secret-keyed password Secret, or pre-create your own
## `authorize` file and inject it via `extraVolumes` / `extraVolumeMounts`.
##
## @param users [array] RADIUS users to render into the `files` module's `authorize` file, in order (FreeRADIUS evaluates the file top-to-bottom with `Fall-Through = no` default semantics, so order matters when two entries could match). Each entry: `name` is REQUIRED (the username — used verbatim, FreeRADIUS accepts any non-whitespace string including `DEFAULT`); `password` is REQUIRED (cleartext when `passwordType` is the default, or the pre-hashed/encoded value matching `passwordType`); `passwordType` (FreeRADIUS check-attribute name — `Cleartext-Password` default, also `Crypt-Password`, `MD5-Password`, `SSHA-Password`, `NT-Password`, etc.); `attributes` (optional list of additional check attributes in raw FreeRADIUS map syntax, e.g. `["NAS-IP-Address == 192.0.2.1"]`); `reply` (optional list of reply attributes in raw map syntax, e.g. `['Filter-Id := "staff"']`). Array shape (vs the map shape used by `clients`) mirrors the other inline-FreeRADIUS-config primitives in this chart (`homeServers[]`, `homeServerPools[]`, `realms[]`, `sites.radsec.clients[]`) and preserves the declaration order that FreeRADIUS's first-match semantics depend on.
## users:
##   - name: alice
##     password: wonderland
##     reply:
##       - 'Filter-Id := "staff"'
##       - 'Framed-IP-Address := "10.0.0.42"'
##   - name: bob
##     password: '$1$abc123$Q2YS.Cb6yYTzS6e9YQyXq.'
##     passwordType: Crypt-Password
##   - name: charlie
##     password: secret
##     attributes:
##       - 'NAS-IP-Address == 192.0.2.1'
##     reply:
##       - 'Service-Type := Framed-User'
##
users: []


## =============================================================================
## @section FreeRADIUS Authentication parameters
## -----------------------------------------------------------------------------
## How FreeRADIUS obtains the various secrets it needs (database password,
## status-site shared secret, RADSEC key passphrase).
## Use these to bring your own Secret instead of the chart-managed one.
## =============================================================================

auth:
  ## @param auth.existingSecret Name of an already-existing Secret containing chart-managed credentials (`database-password`, `mariadb-password`, `sites-status-secret`, `clients-radsec-secret`, and `sites-radsec-privkey-password` / `mods-eap-tls-privkey-password` when the matching `tls.private_key_password` is set under `sites.radsec.tls.*` / `modules.eap.tlsConfig.*`). Overrides chart-rendered Secret rendering.
  ##
  existingSecret: ""
  ## @param auth.existingSecretPerPassword Per-credential override map. Each entry points to a different Secret/key combination — useful when individual passwords are managed by separate operators (e.g. CNPG for `databasePassword`). Recognised keys: `databasePassword`, `mariadbPassword`, `sitesStatusSecret`, `metricsSecret`, `sitesRadsecPrivKeyPassword`, `clientsRadsecSecret`, `modsRestPassword`, `modsEapTlsPrivKeyPassword`.
  ## e.g:
  ## existingSecretPerPassword:
  ##   keyMapping:
  ##     databasePassword: password
  ##     sitesStatusSecret: status-secret
  ##     metricsSecret: exporter-secret
  ##   databasePassword:
  ##     name: freeradius.pocwatt-freeradius-cluster.credentials
  ##   sitesStatusSecret:
  ##     name: freeradius-status
  ##   metricsSecret:
  ##     name: freeradius-exporter
  ##
  existingSecretPerPassword: {}


## =============================================================================
## @section Deployment / Workload parameters
## -----------------------------------------------------------------------------
## Workload-level concerns: how many replicas, rollout strategy, scheduler,
## host aliases, deployment-only annotations, and termination behaviour.
## Pod-level scheduling and security live in the next two sections.
## =============================================================================

## @param kind Workload kind rendered by `templates/Application.yaml`. Allowed (case-insensitive): `Deployment` / `deployment`, `StatefulSet` / `statefulset`, `DaemonSet` / `daemonset` — normalised internally to canonical PascalCase. `Deployment` keeps the existing semantics (shared PVC mounted from `templates/PersistentVolumeClaim.yaml`); `StatefulSet` gives stable pod identity, wires the headless Service via `serviceName`, and switches persistence to per-replica `volumeClaimTemplates` rendered from `persistence.*` (the standalone `PersistentVolumeClaim.yaml` is then skipped); `DaemonSet` schedules one pod per node — `replicaCount` is ignored, `horizontalPodAutoscaler` is silently skipped (no scale subresource), and persistence still mounts the shared `PersistentVolumeClaim.yaml` (requires `RWX` for multi-node pods, or use `existingClaim` / `emptyDir`).
##
kind: Deployment
## @param replicaCount Number of FreeRADIUS pods. Ignored when `horizontalPodAutoscaler.enabled` is true.
##
replicaCount: 1
## Horizontal Pod Autoscaler. When enabled, the chart renders an HPA targeting
## the FreeRADIUS Deployment and stops setting `spec.replicas` on the
## Deployment so the two don't fight. See `templates/HorizontalPodAutoscaler.yaml`.
##
horizontalPodAutoscaler:
  ## @param horizontalPodAutoscaler.enabled Render an HPA and skip setting `replicas` on the Deployment.
  ##
  enabled: false
  ## @param horizontalPodAutoscaler.minReplicas Lower bound for HPA-managed replica count.
  ##
  minReplicas: 1
  ## @param horizontalPodAutoscaler.maxReplicas Upper bound for HPA-managed replica count.
  ##
  maxReplicas: 5
  ## @param horizontalPodAutoscaler.targetCPU Target average CPU utilization (percent). Set to "" to disable CPU-based scaling — be sure to set `targetMemory` in that case (an HPA without metrics is rejected).
  ##
  targetCPU: 80
  ## @param horizontalPodAutoscaler.targetMemory Target average memory utilization (percent). Set to "" to disable memory-based scaling.
  ##
  targetMemory: 80
  ## @param horizontalPodAutoscaler.metrics Full HPA `metrics` list passthrough. When non-empty, takes precedence over `targetCPU`/`targetMemory` and is rendered verbatim — use this for custom/external metrics or any HPA shape the simple shorthand can't express.
  ##
  metrics: []
## @param revisionHistoryLimit Number of old ReplicaSets retained for rollback
##
revisionHistoryLimit: 3
## @param updateStrategy.type Set up update strategy for the FreeRADIUS Deployment.
##
updateStrategy:
  type: RollingUpdate
## @param schedulerName Alternative scheduler
##
schedulerName: ""
## @param terminationGracePeriodSeconds In seconds, time the given to the FreeRADIUS pod needs to terminate gracefully
##
terminationGracePeriodSeconds: ""
## @param priorityClassName Priority class name
##
priorityClassName: ""
## @param podManagementPolicy StatefulSet pod-start ordering. Empty (default) lets Kubernetes apply its built-in `OrderedReady`; set `Parallel` to start all pods simultaneously. IMMUTABLE on existing StatefulSets — Kubernetes rejects updates to this field after creation, so flipping the value on a live release requires `kubectl delete sts --cascade=orphan` (preserves pods + PVCs) before the new spec lands. No effect on Deployment / DaemonSet workloads.
##
podManagementPolicy: ""
## @param hostAliases Add deployment host aliases
##
hostAliases: []
## @param deploymentAnnotations Annotations to add to the deployment object (not the pod template — use `podAnnotations` for the pod)
##
deploymentAnnotations: {}


## =============================================================================
## @section Pod parameters
## -----------------------------------------------------------------------------
## Anything that lives on the PodSpec: labels/annotations on the pod itself,
## affinity/anti-affinity, node selection, tolerations, topology spread, and
## service-account token mounting.
## =============================================================================

## @param automountServiceAccountToken Mount Service Account token in pod
##
automountServiceAccountToken: false
## @param podLabels Extra labels for FreeRADIUS pods
##
podLabels: {}
## @param podAnnotations Extra annotations for FreeRADIUS pods
##
podAnnotations: {}
## @param podAffinityPreset Pod affinity preset. Ignored if `affinity` is set. Allowed values: `soft` or `hard`
##
podAffinityPreset: ""
## @param podAntiAffinityPreset Pod anti-affinity preset. Ignored if `affinity` is set. Allowed values: `soft` or `hard`
##
podAntiAffinityPreset: soft
## Node affinity preset for FreeRADIUS pods. Ignored if `affinity` is set.
##
nodeAffinityPreset:
  ## @param nodeAffinityPreset.type Node affinity preset type. Allowed values: `soft` or `hard`
  ##
  type: ""
  ## @param nodeAffinityPreset.key Node label key to match. Ignored if `affinity` is set.
  ##
  key: ""
  ## @param nodeAffinityPreset.values Node label values to match. Ignored if `affinity` is set.
  ##
  values: []
## @param affinity Affinity for FreeRADIUS pods assignment
##
affinity: {}
## @param nodeSelector Node labels for FreeRADIUS pods assignment
##
nodeSelector: {}
## @param tolerations Tolerations for FreeRADIUS pods assignment
##
tolerations: []
## @param topologySpreadConstraints Topology Spread Constraints for FreeRADIUS pods assignment
##
topologySpreadConstraints: {}


## =============================================================================
## @section Container parameters
## -----------------------------------------------------------------------------
## Anything that lives on the FreeRADIUS container spec: command/args overrides,
## ports, security context, resources, probes, lifecycle hooks, extra volumes,
## and sidecars / init containers.
## =============================================================================

## @param command Override the default container command (useful when the bundled image's entrypoint isn't what you want). Ignored when `diagnosticMode.enabled` is true.
##
command: []
## @param args Override the default container args. Ignored when `diagnosticMode.enabled` is true.
##
args: []
## @param lifecycleHooks lifecycleHooks for the FreeRADIUS container(s) to automate configuration before or after startup
##
lifecycleHooks: {}
## @param containerPorts.auth FreeRADIUS Auth container port (UDP)
## @param containerPorts.acct FreeRADIUS Accounting container port (UDP)
## @param containerPorts.coa FreeRADIUS CoA container port (UDP)
## @param containerPorts.radsec FreeRADIUS RADSEC container port (TCP, TLS)
## @param containerPorts.status FreeRADIUS Status container port (UDP). Cluster-reachable when `metrics.enabled` so the standalone exporter Deployment can scrape it. Locked down to RADIUS pods + the metrics exporter via NetworkPolicy.
## @param containerPorts.metrics Prometheus exporter container port (TCP). Lives on the standalone exporter pod (`templates/metrics/Deployment.yaml`), NOT the FreeRADIUS container.
##
containerPorts:
  auth: 1812
  acct: 1813
  coa: 3799
  radsec: 2083
  status: 18121
  metrics: 9812
## @param extraPorts Extra ports for container deployment
##
extraPorts: []
## FreeRADIUS Pod security context. Defaults are friendly to the upstream
## `freeradius/freeradius-server` image which runs as UID/GID 101.
##
## @param podSecurityContext.enabled Enable security context for FreeRADIUS pods
## @param podSecurityContext.fsGroup Group ID for the mounted volumes' filesystem
## @param podSecurityContext.runAsUser User ID for the FreeRADIUS pod
##
podSecurityContext:
  enabled: true
  fsGroupChangePolicy: Always
  sysctls: []
  supplementalGroups: []
  fsGroup: 101
## FreeRADIUS container security context. Defaults follow the Pod Security
## Standard "restricted" profile (allowPrivilegeEscalation: false, drop ALL
## capabilities, runAsNonRoot, seccomp RuntimeDefault) — flipping `enabled: true`
## immediately yields a hardened container. `SYS_PTRACE` is added because the
## upstream image's `radiusd -fxx` exec mode uses ptrace for debug output.
##
## @param containerSecurityContext.enabled FreeRADIUS container securityContext
## @param containerSecurityContext.capabilities Linux capabilities to add/drop on the FreeRADIUS container
## @param containerSecurityContext.runAsUser User ID for the FreeRADIUS container
## @param containerSecurityContext.runAsNonRoot Set FreeRADIUS container's Security Context runAsNonRoot
## @param containerSecurityContext.readOnlyRootFilesystem Mount the container's root filesystem as read-only. The chart already provides a writable emptyDir mount at `/startechnica/freeradius/tmp`, so this is generally safe.
## @param containerSecurityContext.allowPrivilegeEscalation Block child processes from gaining more privileges than the parent (required by PSS "restricted")
## @param containerSecurityContext.seccompProfile Seccomp profile applied to the container
##
containerSecurityContext:
  enabled: true
  allowPrivilegeEscalation: false
  capabilities:
    add:
      - SYS_PTRACE
    drop: ["ALL"]
  privileged: false
  runAsUser: 101
  runAsNonRoot: true
  readOnlyRootFilesystem: true
  seccompProfile:
    type: RuntimeDefault
  seLinuxOptions: {}

## FreeRADIUS containers' resource requests and limits
## ref: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/
## @param resourcesPreset Set container resources according to one common preset (allowed values: none, nano, micro, small, medium, large, xlarge, 2xlarge). Ignored if `resources` is set.
##
resourcesPreset: "g-2xsmall"
## @param resources Set container requests and limits for different resources like CPU or memory (essential for production workloads)
##
resources: {}

## Configure extra options for FreeRADIUS containers' liveness, readiness and
## startup probes. The chart's probes use `radclient status` against the
## `status` virtual server (`sites.status.enabled`). Disable that block
## or supply `customLivenessProbe` / `customReadinessProbe` if you need
## different checks.
##
## @param startupProbe.enabled Enable startupProbe
## @param startupProbe.initialDelaySeconds Initial delay seconds for startupProbe
## @param startupProbe.periodSeconds Period seconds for startupProbe
## @param startupProbe.timeoutSeconds Timeout seconds for startupProbe
## @param startupProbe.failureThreshold Failure threshold for startupProbe
## @param startupProbe.successThreshold Success threshold for startupProbe
##
startupProbe:
  enabled: false
  exec:
    command:
      - sh
      - /scripts/startup.sh
  initialDelaySeconds: 120
  periodSeconds: 15
  timeoutSeconds: 5
  failureThreshold: 10
  successThreshold: 1
## @param livenessProbe.enabled Enable livenessProbe
## @param livenessProbe.initialDelaySeconds Initial delay seconds for livenessProbe
## @param livenessProbe.periodSeconds Period seconds for livenessProbe
## @param livenessProbe.timeoutSeconds Timeout seconds for livenessProbe
## @param livenessProbe.failureThreshold Failure threshold for livenessProbe
## @param livenessProbe.successThreshold Success threshold for livenessProbe
##
livenessProbe:
  enabled: true
  exec:
    command:
      - sh
      - /scripts/healthcheck.sh
  initialDelaySeconds: 20
  periodSeconds: 60
  timeoutSeconds: 2
  failureThreshold: 3
  successThreshold: 1
## @param readinessProbe.enabled Enable readinessProbe
## @param readinessProbe.initialDelaySeconds Initial delay seconds for readinessProbe
## @param readinessProbe.periodSeconds Period seconds for readinessProbe
## @param readinessProbe.timeoutSeconds Timeout seconds for readinessProbe
## @param readinessProbe.failureThreshold Failure threshold for readinessProbe
## @param readinessProbe.successThreshold Success threshold for readinessProbe
##
readinessProbe:
  enabled: true
  exec:
    command:
      - sh
      - /scripts/healthcheck.sh
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 1
  failureThreshold: 3
  successThreshold: 1
## @param customStartupProbe Override default startup probe for FreeRADIUS containers
##
customStartupProbe: {}
## @param customLivenessProbe Override default liveness probe for FreeRADIUS containers
##
customLivenessProbe: {}
## @param customReadinessProbe Override default readiness probe for FreeRADIUS containers
##
customReadinessProbe: {}
## @param startupWaitOptions Override default builtin startup wait check options for FreeRADIUS containers
##
startupWaitOptions: {}

## @param extraVolumes Additional volumes attached to the FreeRADIUS pod. Pair with `extraVolumeMounts` below.
## extraVolumes:
##  - name: my-volume
##    configMap:
##      name: my-configmap
##
extraVolumes: []
## @param extraVolumeMounts Additional volume mounts attached to the FreeRADIUS container.
##
extraVolumeMounts: []
## @param extraModules [array] Mount additional FreeRADIUS module files from ConfigMaps you provide (the chart does NOT render the content). Each entry: `name` is the on-disk filename + pod volume name, mounted at `mods-enabled/<name>` via `subPath` (valid DNS-1123 label; must not collide with a built-in volume name such as `data`/`temp`/`freeradius-sites` or a built-in module name `sql`/`rest`/`json`/`pam`/`pap`/`eap`); `existingConfigMap` is the ConfigMap to mount; `key` is the data key within it (defaults to `name`).
## extraModules:
##   - name: ldap
##     existingConfigMap: my-ldap-module
##     key: ldap
##
extraModules: []
## @param extraSites [array] Mount additional FreeRADIUS virtual-server (site) files from ConfigMaps you provide (projected into the shared sites volume; the chart does NOT render the content). Each entry: `name` is the on-disk filename at `sites-enabled/<name>` (valid DNS-1123 label, no collision with built-in volume names); `existingConfigMap` is the ConfigMap to mount; `key` is the data key within it (defaults to `name`).
## extraSites:
##   - name: my-vhost
##     existingConfigMap: my-vhost-site
##     key: my-vhost
##
extraSites: []
## Custom `mods-config` data projected into the container under
## `/etc/freeradius/mods-config/<subdir>/`. Each top-level key is a subdirectory
## name; its value is a map of `filename: content` (content is rendered with
## `tpl`). These are DATA files only — REST body templates, policy snippets,
## attribute maps, a `files`-style user list — referenced via `$INCLUDE`/xlat
## from a module instance in `mods-enabled/` or a policy. Dropping a file here
## does NOT load a module on its own; the module instance still lives in
## `mods-enabled/` (e.g. a custom `rest` or `sql` instance).
##
## @param modsConfig [object] Map of mods-config subdirectories to their files (`modsConfig.<subdir>.<filename>`).
## modsConfig:
##   files:
##     authorize: |
##       # mods-config/files/authorize -- rlm_files user entries
##       alice  Cleartext-Password := "s3cret"
##              Reply-Message := "Hello %{User-Name}"
##
modsConfig: {}
## @param initContainers Add additional init containers for the FreeRADIUS pod(s)
##
initContainers: []
## @param sidecars Additional sidecar containers added to the FreeRADIUS pod
##
sidecars: []


## =============================================================================
## @section Persistence parameters
## -----------------------------------------------------------------------------
## PersistentVolumeClaim consumed by `templates/PersistentVolumeClaim.yaml` and
## auto-mounted into the FreeRADIUS container at `persistence.mountPath`.
## When disabled, the chart falls back to an `emptyDir`. The FreeRADIUS image
## runs as UID/GID 101 — make sure the volume is writable by that user
## (or enable `bootstrap.volumePermissions.enabled` to chown it via an init container).
## =============================================================================

## @param persistence.enabled Render a PersistentVolumeClaim AND mount it into the FreeRADIUS container at `persistence.mountPath`.
## @param persistence.existingClaim Name of an externally-managed PVC to reference instead of creating one.
## @param persistence.storageClass StorageClass for the PVC. Defaults to `global.storageClass` when both are empty. Set to `-` to disable dynamic provisioning.
## @param persistence.accessModes List of access modes requested on the PVC.
## @param persistence.size Storage capacity requested on the PVC.
## @param persistence.mountPath Path inside the FreeRADIUS container where the PVC is mounted. Defaults match the upstream image's data path.
## @param persistence.subPath Sub-path within the volume mounted at `mountPath`.
## @param persistence.annotations Extra annotations applied to the PVC. Merged with `commonAnnotations`.
## @param persistence.selector Label selector for binding to a specific pre-provisioned PV.
## @param persistence.dataSource Source the PVC clones from (Snapshot or another PVC).
##
persistence:
  enabled: false
  existingClaim: ""
  storageClass: ""
  accessModes:
    - ReadWriteOnce
  size: 8Gi
  mountPath: /startechnica/freeradius
  subPath: ""
  annotations: {}
  selector: {}
  dataSource: {}


## =============================================================================
## @section Metrics parameters
## -----------------------------------------------------------------------------
## Standalone `bvantagelimited/freeradius_exporter` Deployment. When
## `metrics.enabled: true` the chart renders the exporter as a **separate
## Deployment** (`templates/metrics/Deployment.yaml`) — NOT a sidecar on the
## FreeRADIUS pod. The exporter pod talks to the FreeRADIUS `status` virtual
## server over the in-cluster Service, then exposes Prometheus metrics on
## its own Service (`<release>-freeradius-metrics`) at `service.ports.metrics`.
##
## **Requires `sites.status.enabled: true`** AND `status` reachable on
## the Service (the chart's defaults handle both: `sites.status.listen`
## defaults to `0.0.0.0` and `service.ports.status` is auto-published).
##
## ServiceMonitor / PrometheusRule are only rendered when both `metrics.enabled`
## and the matching sub-block flag are true (and the Prometheus Operator CRDs
## are present). Manifests live under `templates/metrics/`.
## =============================================================================

metrics:
  ## @param metrics.enabled Render the standalone `bvantagelimited/freeradius_exporter` Deployment and Service. Requires `sites.status.enabled: true`.
  ##
  enabled: false
  ## @param metrics.secret Shared secret authenticating the exporter Deployment's RADIUS Status-Server queries. Used by the metrics exporter (`RADIUS_PASSWORD`) AND by the `client exporter { }` block in the status virtual server (`$ENV{FREERADIUS_EXPORTER_SECRET}`). Separate from `sites.status.secret` — independent rotation, independent blast radius. When empty, the chart auto-generates a 16-char random value and persists it in the chart credentials Secret (`<fullname>`) under key `exporter-secret`; user-supplied values take precedence and are honoured on every render.
  ##
  secret: ""
  ## @param metrics.replicaCount Number of exporter pods.
  ##
  replicaCount: 1
  ## FreeRADIUS Prometheus exporter image.
  ## ref: https://github.com/bvantagelimited/freeradius_exporter
  ##
  ## @param metrics.image.registry FreeRADIUS exporter image registry
  ## @param metrics.image.repository FreeRADIUS exporter image repository
  ## @param metrics.image.tag FreeRADIUS exporter image tag
  ## @param metrics.image.digest FreeRADIUS exporter image digest (overrides `tag` when set)
  ## @param metrics.image.pullPolicy FreeRADIUS exporter image pull policy
  ## @param metrics.image.pullSecrets FreeRADIUS exporter image pull secrets
  ##
  image:
    registry: ghcr.io
    repository: bvantagelimited/freeradius_exporter
    tag: "1.4.4"
    digest: ""
    pullPolicy: IfNotPresent
    pullSecrets: []
  ## @param metrics.extraArgs Extra command-line flags passed to the exporter (e.g. `--freeradius.timeout=5s`, `--log.level=debug`).
  ##
  extraArgs: []
  ## @param metrics.extraEnvVars Extra environment variables for the exporter container. Use this for `RADIUS_*` overrides that aren't covered by the defaults below.
  ## E.g.
  ## extraEnvVars:
  ##   - name: RADIUS_USERNAME
  ##     value: monitor
  ##
  extraEnvVars: []
  ## @param metrics.resources Set container requests and limits for the exporter container.
  ##
  resources:
    limits: {}
    requests: {}
  ## Exporter container security context. Defaults to the PSS "restricted"
  ## profile — the bvantagelimited image is a static Go binary and works fine
  ## with `readOnlyRootFilesystem: true` and dropping all capabilities.
  ##
  ## @param metrics.containerSecurityContext.enabled Apply the security context to the exporter container.
  ## @param metrics.containerSecurityContext.runAsUser UID for the exporter container.
  ## @param metrics.containerSecurityContext.runAsNonRoot Reject containers that try to run as root.
  ## @param metrics.containerSecurityContext.allowPrivilegeEscalation Block child processes from gaining more privileges than the parent.
  ## @param metrics.containerSecurityContext.readOnlyRootFilesystem Mount the container's root filesystem as read-only.
  ## @param metrics.containerSecurityContext.capabilities Linux capabilities to add/drop on the exporter container.
  ## @param metrics.containerSecurityContext.seccompProfile Seccomp profile applied to the exporter container.
  ##
  containerSecurityContext:
    enabled: true
    runAsUser: 65534
    runAsGroup: 65534
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    privileged: false
    capabilities:
      drop: ["ALL"]
    seccompProfile:
      type: RuntimeDefault
    seLinuxOptions: {}
  ## Exporter probes (HTTP GET against the `/metrics` endpoint).
  ##
  ## @param metrics.livenessProbe.enabled Enable livenessProbe on the exporter container.
  ## @param metrics.livenessProbe.initialDelaySeconds Initial delay seconds for livenessProbe.
  ## @param metrics.livenessProbe.periodSeconds Period seconds for livenessProbe.
  ## @param metrics.livenessProbe.timeoutSeconds Timeout seconds for livenessProbe.
  ## @param metrics.livenessProbe.failureThreshold Failure threshold for livenessProbe.
  ## @param metrics.livenessProbe.successThreshold Success threshold for livenessProbe.
  ##
  livenessProbe:
    enabled: true
    initialDelaySeconds: 15
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3
    successThreshold: 1
  ## @param metrics.readinessProbe.enabled Enable readinessProbe on the exporter container.
  ## @param metrics.readinessProbe.initialDelaySeconds Initial delay seconds for readinessProbe.
  ## @param metrics.readinessProbe.periodSeconds Period seconds for readinessProbe.
  ## @param metrics.readinessProbe.timeoutSeconds Timeout seconds for readinessProbe.
  ## @param metrics.readinessProbe.failureThreshold Failure threshold for readinessProbe.
  ## @param metrics.readinessProbe.successThreshold Success threshold for readinessProbe.
  ##
  readinessProbe:
    enabled: true
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3
    successThreshold: 1
  ## @param metrics.customLivenessProbe Verbatim override of the exporter livenessProbe.
  ##
  customLivenessProbe: {}
  ## @param metrics.customReadinessProbe Verbatim override of the exporter readinessProbe.
  ##
  customReadinessProbe: {}
  ## @param metrics.podAnnotations Annotations applied to the exporter pod.
  ##
  podAnnotations: {}
  ## @param metrics.podLabels Extra labels applied to the exporter pod.
  ##
  podLabels: {}
  ## @param metrics.nodeSelector Node labels for exporter pod assignment.
  ##
  nodeSelector: {}
  ## @param metrics.tolerations Tolerations for exporter pod assignment.
  ##
  tolerations: []
  ## @param metrics.affinity Affinity for exporter pod assignment.
  ##
  affinity: {}
  ## @param metrics.priorityClassName Priority class name for the exporter pod.
  ##
  priorityClassName: ""
  ## Exporter Service (chart-rendered as `<fullname>-metrics`, separate from
  ## the main FreeRADIUS Service). Port number is sourced from the top-level
  ## `service.ports.metrics` / `service.nodePorts.metrics` so the chart has a
  ## single ports inventory; this block only carries the Service-type knobs.
  ##
  service:
    ## @param metrics.service.type Kubernetes service type for the exporter.
    ##
    type: ClusterIP
    ## @param metrics.service.clusterIP Explicit clusterIP for the exporter Service.
    ##
    clusterIP: ""
    ## @param metrics.service.loadBalancerIP `loadBalancerIP` when `type` is `LoadBalancer`.
    ##
    loadBalancerIP: ""
    ## @param metrics.service.loadBalancerSourceRanges Allowed CIDRs when `type` is `LoadBalancer`.
    ##
    loadBalancerSourceRanges: []
    ## @param metrics.service.annotations Extra annotations merged into the exporter Service. Use this for Prometheus scrape annotations on clusters not running the Prometheus Operator.
    ##
    annotations: {}
  ## ServiceMonitor (Prometheus Operator) configuration.
  ##
  serviceMonitor:
    ## @param metrics.serviceMonitor.enabled Create a ServiceMonitor resource for the Prometheus Operator
    ##
    enabled: false
    ## @param metrics.serviceMonitor.namespace Namespace in which to create the ServiceMonitor (defaults to the release namespace)
    ##
    namespace: ""
    ## @param metrics.serviceMonitor.jobLabel The label on the target Service used by Prometheus as the job name
    ##
    jobLabel: ""
    ## @param metrics.serviceMonitor.port Service port the ServiceMonitor scrapes
    ##
    port: metrics
    ## @param metrics.serviceMonitor.endpoints [array] Endpoint configuration of the ServiceMonitor. Path is mandatory.
    ##
    endpoints:
      - path: /metrics
    ## @param metrics.serviceMonitor.path HTTP path the exporter exposes
    ##
    path: /metrics
    ## @param metrics.serviceMonitor.interval Scrape interval
    ##
    interval: ""
    ## @param metrics.serviceMonitor.scrapeTimeout Per-scrape timeout
    ##
    scrapeTimeout: ""
    ## @param metrics.serviceMonitor.honorLabels Honor labels coming from the scraped target
    ##
    honorLabels: false
    ## @param metrics.serviceMonitor.labels Extra labels for the ServiceMonitor resource
    ##
    labels: {}
    ## @param metrics.serviceMonitor.annotations Extra annotations for the ServiceMonitor resource
    ##
    annotations: {}
    ## @param metrics.serviceMonitor.selector Selector to filter which Services this ServiceMonitor matches (merged with chart defaults)
    ##
    selector: {}
    ## @param metrics.serviceMonitor.relabelings RelabelConfigs to apply before scraping
    ##
    relabelings: []
    ## @param metrics.serviceMonitor.metricRelabelings MetricRelabelConfigs to apply on collected samples
    ##
    metricRelabelings: []
    ## @param metrics.serviceMonitor.tlsConfig TLS configuration for the scrape endpoint
    ##
    tlsConfig: {}
  ## Prometheus Operator PrometheusRule configuration. The default rule set
  ## targets `bvantagelimited/freeradius_exporter` metric names — the most
  ## common FreeRADIUS exporter (`freeradius_up`, `freeradius_total_*`,
  ## `freeradius_queue_use_percentage`). Override `rules` (or set it to `[]`)
  ## when running a different exporter.
  ##
  prometheusRule:
    ## @param metrics.prometheusRule.enabled If `true`, creates a Prometheus Operator PrometheusRule (also requires `metrics.enabled`, and makes little sense without `metrics.serviceMonitor.enabled`).
    ##
    enabled: false
    ## @param metrics.prometheusRule.namespace Namespace in which to create the PrometheusRule. Defaults to the release namespace; set this when your Prometheus instance's `ruleNamespaceSelector` only watches a specific namespace.
    ##
    namespace: ""
    ## @param metrics.prometheusRule.additionalLabels Extra labels merged onto the PrometheusRule resource so the Prometheus Operator's `ruleSelector` picks it up. Defaults to the kube-prometheus-stack selector labels (`app: prometheus-operator` / `release: prometheus`) — adjust to match your operator install.
    ##
    additionalLabels:
      app: prometheus-operator
      release: prometheus
    ## @param metrics.prometheusRule.groups Verbatim `spec.groups` list passthrough. When non-empty, these groups are rendered alongside the chart's default group built from `rules` (the two are additive, not mutually exclusive). Use this when you need multiple groups with different `interval` / `partial_response_strategy` settings instead of the flat-`rules` shape.
    ##
    groups: []
    ## @param metrics.prometheusRule.rules PrometheusRule alert/recording rules rendered under a single group named after the chart fullname. The list is evaluated as a template, so `{{ include "st-common.names.fullname" . }}` and `{{ `{{` }} $labels.instance {{ `}}` }}`-style placeholders work. The default set targets `bvantagelimited/freeradius_exporter` metric names — override or set to `[]` on other exporters.
    ##
    rules:
      - alert: FreeRADIUSDown
        expr: up{job=~".*{{ include "st-common.names.fullname" . }}.*"} == 0
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: FreeRADIUS instance is down
          description: |
            The FreeRADIUS metrics endpoint on {{ `{{` }} $labels.instance {{ `}}` }} has been unreachable for the last 5 minutes.
            AAA traffic is likely affected — verify the pod is running and the exporter sidecar is healthy.
      - alert: FreeRADIUSAbsent
        expr: absent(up{job=~".*{{ include "st-common.names.fullname" . }}.*"} == 1)
        for: 10m
        labels:
          severity: critical
        annotations:
          summary: FreeRADIUS scrape target has disappeared
          description: |
            Prometheus has not seen any scrape target matching the FreeRADIUS Service for 10 minutes.
            Check the ServiceMonitor selector, the metrics Service, and that the exporter sidecar is listening on `containerPorts.metrics`.
      - alert: FreeRADIUSAuthRejectRateHigh
        ## Metric source: bvantagelimited/freeradius_exporter
        expr: |
          sum by (instance) (rate(freeradius_total_responses_access_reject[5m]))
            /
          clamp_min(sum by (instance) (rate(freeradius_total_requests[5m])), 1)
            > 0.5
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: FreeRADIUS Access-Reject rate above 50%
          description: |
            More than half of the authentication requests on {{ `{{` }} $labels.instance {{ `}}` }} have been rejected over the last 5 minutes.
            Investigate `clients.conf`, user credentials, or backend SQL/LDAP availability.
      - alert: FreeRADIUSAuthRequestsDropped
        ## Metric source: bvantagelimited/freeradius_exporter
        expr: rate(freeradius_total_auth_responses_dropped_requests[5m]) > 0
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: FreeRADIUS is dropping authentication requests
          description: |
            FreeRADIUS on {{ `{{` }} $labels.instance {{ `}}` }} has been dropping auth requests for the last 5 minutes — the server is overloaded or the worker queue is saturated.
            Consider raising the replica count (or HPA bounds) and tuning thread/worker counts in `radiusd.conf`.
      - alert: FreeRADIUSQueueSaturated
        ## Metric source: bvantagelimited/freeradius_exporter
        expr: freeradius_queue_use_percentage > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: FreeRADIUS internal queue >80% utilised
          description: |
            The FreeRADIUS worker queue on {{ `{{` }} $labels.instance {{ `}}` }} has been above 80% utilisation for 5 minutes — requests are queueing up.
            Capacity is close to exhaustion; scale out or tune the thread pool before drops start.


## =============================================================================
## @section Traffic Exposure parameters
## -----------------------------------------------------------------------------
## How traffic reaches FreeRADIUS from outside the pod: the Service (UDP
## auth/acct/coa/status + TCP RADSEC) and optional Pod Disruption Budget. The
## Gateway API / Istio integration lives further down. There is intentionally
## no `ingress:` block — RADIUS is not HTTP and Ingress controllers cannot
## carry UDP traffic.
## =============================================================================

## FreeRADIUS service parameters
##
service:
  ## @param service.type FreeRADIUS Kubernetes service type
  ##
  type: ClusterIP
  ## @param service.ports.auth FreeRADIUS Authentication service port (UDP)
  ## @param service.ports.acct FreeRADIUS Accounting service port (UDP)
  ## @param service.ports.coa FreeRADIUS CoA service port (UDP)
  ## @param service.ports.radsec FreeRADIUS RADSEC service port (TCP, TLS)
  ## @param service.ports.status FreeRADIUS Status service port (UDP). Only published when the metrics exporter Deployment needs to reach it (`metrics.enabled: true`); restrict external access via NetworkPolicy.
  ## @param service.ports.metrics Prometheus exporter service port (TCP). Published on the standalone exporter Service (`<fullname>-metrics`), NOT the main FreeRADIUS Service.
  ##
  ports:
    auth: 1812
    acct: 1813
    coa: 3799
    radsec: 2083
    status: 18121
    metrics: 9812
  ## @param service.nodePorts.auth NodePort for the Auth port (LoadBalancer / NodePort types only)
  ## @param service.nodePorts.acct NodePort for the Accounting port
  ## @param service.nodePorts.coa NodePort for the CoA port
  ## @param service.nodePorts.radsec NodePort for the RADSEC port
  ## @param service.nodePorts.status NodePort for the Status port (rarely needed — exporter reaches the status site through the in-cluster Service)
  ## @param service.nodePorts.metrics NodePort for the exporter Service (when `metrics.service.type` is `NodePort` or `LoadBalancer`)
  ##
  nodePorts:
    auth: ""
    acct: ""
    coa: ""
    radsec: ""
    status: ""
    metrics: ""
  ## @param service.clusterIP FreeRADIUS Kubernetes service clusterIP IP
  ##
  clusterIP: ""
  ## @param service.loadBalancerIP FreeRADIUS loadBalancerIP if service type is `LoadBalancer`
  ##
  loadBalancerIP: ""
  ## @param service.ipFamilyPolicy FreeRADIUS Kubernetes service ipFamilyPolicy policy
  ##
  ipFamilyPolicy: SingleStack
  ## @param service.externalTrafficPolicy Enable client source IP preservation
  ##
  externalTrafficPolicy: Cluster
  ## @param service.allocateLoadBalancerNodePorts Allow users to disable node ports for Service Type=LoadBalancer.
  ##
  allocateLoadBalancerNodePorts: "false"
  ## @param service.loadBalancerClass Enables to use a load balancer implementation other than the cloud provider default.
  ##
  loadBalancerClass: ""
  ## @param service.loadBalancerSourceRanges Addresses allowed when FreeRADIUS service is `LoadBalancer`
  ##
  loadBalancerSourceRanges: []
  ## @param service.extraPorts Extra ports to expose (normally used with the `sidecars` value)
  ##
  extraPorts: []
  ## @param service.annotations Additional Service annotations
  ##
  annotations: {}
  ## @param service.sessionAffinity Session Affinity for Kubernetes service, can be "None" or "ClientIP"
  ##
  sessionAffinity: None
  ## @param service.sessionAffinityConfig Additional settings for the sessionAffinity
  ##
  sessionAffinityConfig: {}
  ## Sibling headless (`clusterIP: None`) Service. Rendered automatically when
  ## `kind: StatefulSet` and skipped when `kind: Deployment` — a StatefulSet
  ## needs it for stable per-pod DNS (`<fullname>-0.<headless>.<ns>.svc.cluster.local`),
  ## a Deployment has no use for one. The chart's main Service still handles
  ## client traffic (auth/acct/coa/radsec/status); this one only backs the
  ## StatefulSet's `serviceName` and per-pod A records. Same selector and same
  ## UDP/TCP ports as the main Service, mirrored from `containerPorts.*`, so
  ## proxy realm fail-over targeting a specific pod works.
  ##
  headless:
    ## @param service.headless.nameSuffix Suffix appended to `<fullname>` to form the headless Service name (final name: `<fullname>-<suffix>`).
    ##
    nameSuffix: "headless"
    ## @param service.headless.publishNotReadyAddresses Include not-yet-ready pods in the headless DNS records. Helpful when downstream callers want to reach a specific ordinal during startup; safe for StatefulSet identity but not for load-balanced traffic.
    ##
    publishNotReadyAddresses: false

## Pod disruption budget configuration
##
podDisruptionBudget:
  ## @param podDisruptionBudget.create Specifies whether a Pod disruption budget should be created
  ##
  create: false
  ## @param podDisruptionBudget.minAvailable Minimum number / percentage of pods that should remain scheduled
  ##
  minAvailable: 1
  ## @param podDisruptionBudget.maxUnavailable Maximum number / percentage of pods that may be made unavailable
  ##
  maxUnavailable: ""


## =============================================================================
## @section TLS configuration
## -----------------------------------------------------------------------------
## In-pod TLS for the RADSEC server. Independent of `modules.sql.tls.*`
## (which is TLS for the SQL backend connection) and of `gateway.tls.*` (which
## terminates at the Gateway). Setting `tls.enabled: true` here enables the
## RADSEC port on the container/Service AND mounts the leaf certificate so
## FreeRADIUS itself serves RADSEC.
##
## Cert-manager-driven issuance is configured under `tls.certManager.*` — a
## single canonical location consumed by both the in-pod RADSEC `Certificate`
## and the gateway-namespace `Certificate` (when `gateway.tls.enabled`).
##
## Auto-generation is implicit: when `tls.enabled: true` and no
## `certificatesSecret` / `existingSecretName` is supplied, the chart issues
## TLS material via cert-manager when its API is detected — bootstrapping its
## own CA chain (`templates/Issuer.yaml`) when no `certManager.issuerRef.name`
## is set — and otherwise renders a chart-generated self-signed genCA leaf
## via `templates/secrets/tls.yaml`.
## =============================================================================

tls:
  ## @param tls.enabled Enable RADSEC (TLS over TCP RADIUS). Opens `containerPorts.radsec` / `service.ports.radsec` on the pod and the Service, mounts the leaf cert via `templates/secrets/tls.yaml`, and (when `gateway.enabled`) wires the istio/gateway-api TLS-passthrough listener.
  ##
  enabled: false
  ## @param tls.autoGenerated [DEPRECATED — no longer consulted] When `tls.enabled: true` the chart now auto-generates RADSEC TLS material implicitly (mechanism: cert-manager when its API is detected, otherwise genCA; suppressed only by `tls.certificatesSecret` / `tls.existingSecretName`). Slated for removal in the next major bump.
  ##
  autoGenerated: false
  ## @param tls.certificatesMountPath Path inside the container where the RADSEC TLS material is mounted
  ##
  certificatesMountPath: /opt/startechnica/freeradius/certs
  ## @param tls.certificatesSecret Name of an existing Secret holding `tls.crt`/`tls.key`/`ca.crt` for RADSEC. Overrides chart-managed Secret rendering.
  ##
  certificatesSecret: ""
  ## @param tls.certFilename Filename of the leaf certificate inside the mounted Secret (defaults to `tls.crt`)
  ##
  certFilename: ""
  ## @param tls.certKeyFilename Filename of the leaf key inside the mounted Secret (defaults to `tls.key`)
  ##
  certKeyFilename: ""
  ## @param tls.certCAFilename Filename of the CA certificate inside the mounted Secret (defaults to `ca.crt`)
  ##
  certCAFilename: ""
  ## @param tls.existingSecretName [DEPRECATED — use `tls.certificatesSecret`] Legacy alias retained for backwards compatibility. Slated for removal in the next major bump.
  ##
  existingSecretName: ""
  ## Cert-manager driven TLS certificate issuance. Single canonical location
  ## consumed by `templates/Certificate.yaml` for both the in-pod RADSEC
  ## certificate (release-namespace block) and the gateway-namespace cert
  ## (when `gateway.enabled && gateway.tls.enabled`).
  ##
  certManager:
    ## @param tls.certManager.create [DEPRECATED — no longer consulted] The chart now uses cert-manager whenever its API is detected; there is no longer a way to force the in-template genCA path on a cluster where cert-manager is installed. To opt out of cert-manager issuance for this release, pre-create a Secret and point `tls.certificatesSecret` at it. Slated for removal in the next major bump.
    ##
    create: true
    ## @param tls.certManager.issuerRef Reference to a pre-existing cert-manager Issuer / ClusterIssuer that signs the leaf certificate(s).
    ##   Leave `name` empty (with `tls.enabled: true` and no `tls.certificatesSecret`) to have the chart bootstrap its OWN self-signed CA via
    ##   cert-manager: a self-signed `Issuer` → a CA `Certificate` (`isCA: true`) → a CA `Issuer`
    ##   (`templates/Issuer.yaml`), with the RADSEC and gateway leaves referencing that chart-managed CA Issuer.
    ##   Set `name` (with `group`/`kind`) to use an existing issuer instead and skip the bootstrap.
    ##
    issuerRef:
      group: cert-manager.io
      kind: ClusterIssuer
      name: ""
    ## Self-signed CA the chart bootstraps when `issuerRef.name` is empty. The CA `Issuer` is namespaced in the
    ## release namespace, so it can sign the gateway leaf only when the gateway `Certificate` shares that namespace
    ## (the default — see `gateway.gateway.namespace`). For a gateway in a different namespace, supply a
    ## `ClusterIssuer` via `issuerRef` instead. Ignored when `issuerRef.name` is set.
    ##
    ## @param tls.certManager.ca.commonName Subject CN of the bootstrapped CA certificate. Defaults to `<fullname> CA`.
    ## @param tls.certManager.ca.duration Validity of the bootstrapped CA certificate.
    ## @param tls.certManager.ca.renewBefore Renew the CA this far before expiry (empty = cert-manager default).
    ## @param tls.certManager.ca.secretName Secret holding the bootstrapped CA key/cert. Defaults to `<fullname>-ca`.
    ##
    ca:
      commonName: ""
      duration: 87600h
      renewBefore: ""
      secretName: ""


## =============================================================================
## @section RBAC parameters
## -----------------------------------------------------------------------------
## ServiceAccount and Role/RoleBinding generation for the FreeRADIUS pod, plus
## the NetworkPolicy controlling who can reach the auth/acct/coa/radsec ports.
## =============================================================================

## ServiceAccount used by the FreeRADIUS pod
##
serviceAccount:
  ## @param serviceAccount.create Enable the creation of a ServiceAccount for FreeRADIUS pods
  ##
  create: true
  ## @param serviceAccount.name Name of the created ServiceAccount
  ##
  name: ""
  ## @param serviceAccount.automountServiceAccountToken Auto-mount the service account token in the pod
  ##
  automountServiceAccountToken: false
  ## @param serviceAccount.annotations Additional custom annotations for the ServiceAccount
  ##
  annotations: {}

## Role / RoleBinding for the FreeRADIUS ServiceAccount. Off by default — the
## upstream image doesn't need to read cluster state, but some sidecars do.
##
rbac:
  ## @param rbac.create Specify whether Role / RoleBinding resources should be created and used
  ##
  create: false
  ## @param rbac.rules Custom RBAC rules (when empty the chart uses a minimal `get/list/watch` on secrets+configmaps)
  ##
  rules: []

## Network Policy. RADIUS is UDP-heavy, so a strict policy is recommended in
## multi-tenant clusters. The chart auto-opens UDP auth/acct (always) and
## conditionally opens UDP coa, UDP status, TCP RADSEC, and TCP metrics based
## on the matching feature flags.
##
networkPolicy:
  ## @param networkPolicy.enabled Render a NetworkPolicy
  ##
  enabled: true
  ## @param networkPolicy.allowExternal When true, allow connections from any source. When false, restrict ingress to pods with the chart's `<fullname>-client: "true"` label and to other FreeRADIUS replicas.
  ##
  allowExternal: true
  ## @param networkPolicy.allowExternalEgress Allow the pod to access any range of port and all destinations.
  ##
  allowExternalEgress: true
  ## @param networkPolicy.extraIngress Add extra ingress rules to the NetworkPolicy
  ##
  extraIngress: []
  ## @param networkPolicy.extraEgress Add extra egress rules to the NetworkPolicy
  ##
  extraEgress: []
  ## @param networkPolicy.ingressNSMatchLabels Labels to match to allow traffic from other namespaces
  ## @param networkPolicy.ingressNSPodMatchLabels Pod labels to match to allow traffic from other namespaces
  ##
  ingressNSMatchLabels: {}
  ingressNSPodMatchLabels: {}
  ## @param networkPolicy.additionalRules Additional NetworkPolicy rules (note: all rules are OR-ed)
  ##
  additionalRules: {}


## =============================================================================
## @section Gateway API parameters
## -----------------------------------------------------------------------------
## Optional Gateway integration. When `gateway.enabled` is true the chart
## renders one of two stacks based on `gateway.implementation`:
##   - "gateway-api" → Kubernetes Gateway API (Gateway + UDPRoute + TLSRoute + ReferenceGrant)
##   - "istio"       → Istio CRDs (networking.istio.io Gateway + VirtualService)
##
## RADIUS traffic shapes that map onto Gateway listeners:
##   - UDP auth (1812) / acct (1813) / coa (3799) → UDPRoute (gateway-api),
##     UDP listener (istio Gateway). UDPRoute support varies by implementation
##     (Cilium / Envoy Gateway support it; Istio currently does not — use the
##     istio path for RADIUS UDP if you're running Istio).
##   - TCP RADSEC (2083, TLS) → TLSRoute passthrough (gateway-api) or
##     `protocol: TLS, mode: PASSTHROUGH` listener (istio Gateway).
## =============================================================================

gateway:
  ## @param gateway.enabled Enable the creation of Gateway resources to manage incoming traffic to FreeRADIUS.
  ##
  enabled: false
  ## @param gateway.implementation Gateway implementation to render. Allowed values: `gateway-api` or `istio`.
  ##
  implementation: gateway-api
  ## @param gateway.infrastructure Optional Gateway data-plane infrastructure extension. When set, the chart renders the matching infrastructure CR and attaches it to its Gateway via `spec.infrastructure.parametersRef`. Only applicable when `implementation: gateway-api`. Allowed values: `""` (none — Gateway uses its GatewayClass defaults) or `envoy` (renders an `EnvoyProxy` resource under `gateway.envoyproxy.io/v1alpha1`; pair with an Envoy Gateway-backed `gatewayClassName` such as `eg`). `gatewayClassName` is independent — set it to whichever GatewayClass backs your Envoy Gateway install.
  ##
  infrastructure: envoy

  ## ---- Gateway resource -----------------------------------------------------
  ## Configuration for the Gateway resource itself (created here, or referenced
  ## externally via `existingGateway`).
  ##
  gateway:
    ## @param gateway.gateway.create Create a Gateway API Gateway resource. When false the chart expects an existing Gateway to be referenced via `existingGateway` or per-route `parentRefs`.
    ##
    create: false
    ## @param gateway.gateway.name Name of the Gateway resource to create. Defaults to `<fullname>-gateway` (the `-gateway` suffix avoids a name collision with the chart's Service and Deployment — important under Envoy Gateway and Istio gateway-api, where the controller materialises an Envoy/Istio data-plane Deployment named after the Gateway in the same namespace).
    ##
    name: ""
    ## @param gateway.gateway.namespace Namespace for the Gateway resource. Defaults to the release namespace.
    ##
    namespace: ""

  ## ---- EnvoyProxy (data-plane customization for Envoy Gateway) -------------
  ## Consumed only when `gateway.infrastructure: envoy` AND
  ## `implementation: gateway-api`. Same `{create, name}` shape as
  ## `gateway.gateway.*`: `create: true` renders the chart's own EnvoyProxy
  ## manifest in the gateway namespace; `create: false` is BYO and references
  ## an externally-managed EnvoyProxy by `name`. Either way, the chart's
  ## Gateway (when it owns one) wires `spec.infrastructure.parametersRef` to
  ## the resolved name.
  ##
  envoyProxy:
    ## @param gateway.envoyProxy.create Render the chart's EnvoyProxy manifest (`templates/gateway-api/EnvoyProxy.yaml`). When `true`, the chart owns the resource with a bundled opinionated spec (Kubernetes provider, 1 replica, JSON access-log telemetry to stdout); to tune the spec further than what the template exposes, switch to BYO (`create: false`) and manage the EnvoyProxy CR externally. When `false`, the chart skips rendering and uses `gateway.envoyProxy.name` for the Gateway's `parametersRef`.
    ##
    create: true
    ## @param gateway.envoyProxy.name Name of the EnvoyProxy resource. When `create: true`, empty defaults to the chart fullname. When `create: false`, **required** — must reference an EnvoyProxy that exists in `gateway.gateway.namespace`.
    ##
    name: ""

  ## @param gateway.proxyProtocol End-to-end PROXY-protocol v1 wiring for RADSEC. When `true` AND `gateway.implementation: gateway-api` AND `gateway.infrastructure: envoy` AND `tls.enabled: true`, the chart (1) renders an Envoy Gateway `BackendTrafficPolicy` (`gateway.envoyproxy.io/v1alpha1`) targeting the FreeRADIUS Service's `tls-radsec` port with `proxyProtocol.version: V1` (Envoy prepends PROXY v1 to every TCP connection to the pod), and (2) forces `proxy_protocol = yes` on the RADSEC `listen { }` block so FreeRADIUS parses the header and uses the real client IP for `Packet-Src-IP-Address` / `clients.conf` lookup. v1 is pinned because FreeRADIUS 3.2.x only parses v1 on its TCP listeners (v2 is undocumented in the receive path). UDP auth/acct/coa is untouched — PROXY protocol only applies to TCP, and FreeRADIUS UDP listeners can't parse it regardless. (To wire PROXY-protocol parsing without Envoy Gateway — e.g. behind HAProxy — set `sites.radsec.listen.proxy_protocol: true` independently.)
  ##
  proxyProtocol: false

  ## @param gateway.clusterDomain Cluster domain used by the VirtualService backend FQDN. Falls back to the top-level `clusterDomain` when empty.
  ##
  clusterDomain: ""
  ## @param gateway.serviceAccountName Service Account name attached to the Gateway data-plane workload (Istio Ambient / gateway-api implementations that respect it)
  ##
  serviceAccountName: ""
  ## @param gateway.selector Gateway instance selector labels (Istio)
  ##
  selector: {}
  ## @param gateway.gatewayClassName Cluster-scoped GatewayClass that backs the Gateway (gateway-api implementations). Default `istio` matches Istio's bundled gateway-api GatewayClass; set to `eg` for Envoy Gateway, `cilium` for Cilium, etc. Independent of `gateway.implementation` and `gateway.infrastructure` — the chart does not derive this from either knob.
  ##
  gatewayClassName: eg
  ## @param gateway.listeners Verbatim `listeners:` list rendered on the chart's Gateway. When non-empty, the chart skips its default UDP auth/acct/coa + TLS-passthrough listener generation.
  ##
  listeners: []
  ## @param gateway.existingGateway Name of an existing Gateway resource to attach the chart's routes to (skips Gateway rendering).
  ##
  existingGateway: ~
  ## @param gateway.hostnames Hostnames (or SNI names for RADSEC) the chart's routes / listeners should match. The first entry seeds the cert-manager `Certificate` for the gateway-namespace cert. Empty means "match all" where the API allows it.
  ##
  hostnames: []

  ## ---- TLS ------------------------------------------------------------------
  ## TLS configuration applied to the default Gateway listener generation and
  ## to the chart-managed TLS Secret rendered by `templates/secrets/gateway-tls.yaml`.
  ## The certificate Secret name is computed by the `freeradius.gateway.tlsSecretName`
  ## helper — single source of truth shared by both Gateway templates and the
  ## gateway-tls Secret, so listener and secret can never drift apart.
  ##
  tls:
    ## @param gateway.tls.enabled Render a TLS listener on the default Gateway (Istio: `protocol: TLS, mode: PASSTHROUGH`; gateway-api: TLS passthrough via TLSRoute). Has no effect when `gateway.listeners` is set. The chart auto-derives whether to terminate vs passthrough based on `tls.enabled` (in-pod RADSEC) — when `tls.enabled` is true the listener is passthrough.
    ##
    enabled: false
    ## @param gateway.tls.existingSecret Name of an externally-managed Kubernetes Secret holding the TLS material the Gateway TLS listener references (only relevant when the listener uses `mode: SIMPLE` / Terminate).
    ##
    existingSecret: ""
    ## @param gateway.tls.selfSigned Generate a self-signed TLS Secret in the gateway's namespace via `templates/secrets/gateway-tls.yaml`. Skipped when `gateway.tls.existingSecret` / `gateway.tls.secrets` are set, or when cert-manager issuance is active (the cert-manager API is detected on the cluster) — in which case the gateway leaf is issued by `templates/Certificate.yaml` instead.
    ##
    selfSigned: false
    ## @param gateway.tls.secrets Custom TLS certificates rendered as Secrets in the gateway's namespace. 'name' should match what the Gateway listener references.
    ##
    secrets: []

  ## ---- TLSRoute (RADSEC) ----------------------------------------------------
  ## Routing rules for the RADSEC TLS passthrough listener. Only used when
  ## `implementation: gateway-api` AND `tls.enabled` is true.
  ##
  tlsRoute:
    ## @param gateway.tlsRoute.parentRefs Explicit `parentRefs` override for the TLSRoute. When empty, defaults to the chart-rendered ListenerSet (if any) or the chart's Gateway.
    ##
    parentRefs: []

  ## ---- UDPRoute (auth/acct/coa) --------------------------------------------
  ## Routing rules for the UDP RADIUS listeners. Only used when
  ## `implementation: gateway-api` AND the cluster's GatewayClass supports
  ## UDPRoute (Envoy Gateway / Cilium do; Istio does not — fall back to the
  ## istio path on those clusters).
  ##
  udpRoute:
    ## @param gateway.udpRoute.enabled Render UDPRoute resources for auth/acct (and coa when `sites.coa.enabled`).
    ##
    enabled: true
    ## @param gateway.udpRoute.parentRefs Explicit `parentRefs` override for the UDPRoutes. When empty, defaults to the chart-rendered ListenerSet (if any) or the chart's Gateway.
    ##
    parentRefs: []

  ## ---- VirtualService (istio) ----------------------------------------------
  ## Istio VirtualService passthrough overrides. Only used when
  ## `implementation: istio`. The default VirtualService renders a TLS
  ## passthrough rule for RADSEC when `tls.enabled` is true; this block lets
  ## the user override raw TLS/TCP route lists or bring an existing one.
  ##
  virtualService:
    ## @param gateway.virtualService.existingVirtualService Name of an existing VirtualService to use (skips VirtualService rendering)
    ##
    existingVirtualService: ~
    ## @param gateway.virtualService.tls Verbatim `spec.tls` list passthrough. When non-empty, overrides the chart's auto-derived TLS-passthrough rule.
    ##
    tls: []
    ## @param gateway.virtualService.tcp Verbatim `spec.tcp` list passthrough. Useful for non-TLS TCP routes the chart doesn't render by default.
    ##
    tcp: []

  ## ---- ReferenceGrant ------------------------------------------------------
  ## Cross-namespace permission for the gateway-api routes to point at the
  ## FreeRADIUS Service when the Gateway is in a different namespace.
  ##
  referenceGrant:
    ## @param gateway.referenceGrant.enabled Render a ReferenceGrant allowing routes in `gateway.gateway.namespace` to target the chart's Service.
    ##
    enabled: false
    ## @param gateway.referenceGrant.from Verbatim `spec.from` override
    ##
    from: []
    ## @param gateway.referenceGrant.to Verbatim `spec.to` override
    ##
    to: []

  ## ---- ListenerSet ---------------------------------------------------------
  ## Attach extra listeners to the parent Gateway via a ListenerSet
  ## (`gateway.networking.k8s.io` v1 / x-k8s.io v1alpha1 XListenerSet). When
  ## enabled with non-empty `listeners`, the chart's UDPRoute / TLSRoute
  ## default `parentRefs` switch to this ListenerSet automatically.
  ##
  listenerSet:
    ## @param gateway.listenerSet.enabled Render a ListenerSet
    ##
    enabled: false
    ## @param gateway.listenerSet.parentRef Verbatim `spec.parentRef` override
    ##
    parentRef: {}
    ## @param gateway.listenerSet.listeners `listeners:` list rendered on the ListenerSet
    ##
    listeners: []


## =============================================================================
## @section Database parameters
## -----------------------------------------------------------------------------
## Backend storage for the FreeRADIUS SQL module. Pick exactly one of:
##   - `mariadb.enabled: true`     → bundled Bitnami MariaDB subchart (mysql dialect)
##   - `postgresql.enabled: true`  → bundled Bitnami PostgreSQL subchart (postgresql dialect)
##   - `externalDatabase.host: …`  → external database; dialect is whichever
##                                   `modules.sql.dialect` you set
##   - none of the above + `modules.sql.dialect: sqlite` → file-based, no
##                                   network backend (see `modules.sql.sqlite.*`)
##
## The chart's hard validator (`freeradius.validate.sql.backend`) rejects:
##   - both subcharts enabled at once,
##   - a subchart whose wire protocol doesn't match `modules.sql.dialect`,
##   - `dialect: sqlite` with any subchart enabled,
##   - `dialect: mysql|postgresql` with no subchart AND empty `externalDatabase.host`.
##
## Connection details flow into the SQL module via the chart-managed envvars
## ConfigMap, sourced through the `freeradius.sql.{host,port,name,user,
## secretName,secretKey}` helpers — single source of truth for both the SQL
## module env-vars and the `db-bootstrap` init container.
## =============================================================================

## MariaDB chart configuration. Enable for the mysql dialect.
## ref: https://github.com/bitnami/charts/blob/master/bitnami/mariadb/values.yaml
##
## @param mariadb.enabled Switch to enable or disable the bundled MariaDB subchart. Requires `modules.sql.dialect: mysql`.
## @param mariadb.auth.username Name for a custom user to create
## @param mariadb.auth.password Password for the custom user to create
## @param mariadb.auth.database Name for a custom database to create
## @param mariadb.auth.existingSecret Name of existing secret to use for MariaDB credentials
## @param mariadb.architecture MariaDB architecture (`standalone` or `replication`)
##
mariadb:
  enabled: false
  global:
    security:
      allowInsecureImages: true
  auth:
    username: freeradius_user
    password: ""
    database: freeradius_db
    existingSecret: ""
  architecture: standalone

## PostgreSQL chart configuration. Enable for the postgresql dialect.
## ref: https://github.com/bitnami/charts/blob/master/bitnami/postgresql/values.yaml
##
## @param postgresql.enabled Switch to enable or disable the bundled PostgreSQL subchart. Requires `modules.sql.dialect: postgresql`.
## @param postgresql.auth.username Name for a custom user to create
## @param postgresql.auth.password Password for the custom user to create
## @param postgresql.auth.database Name for a custom database to create
## @param postgresql.auth.existingSecret Name of existing secret to use for PostgreSQL credentials. Must contain the key referenced by `postgresql.auth.secretKeys.userPasswordKey` (default `password`).
## @param postgresql.architecture PostgreSQL architecture (`standalone` or `replication`)
##
postgresql:
  enabled: false
  global:
    security:
      allowInsecureImages: true
  auth:
    username: freeradius_user
    password: ""
    database: freeradius_db
    existingSecret: ""
  architecture: standalone

## External Database configuration. Used when neither `mariadb.enabled` nor
## `postgresql.enabled` is true. Dialect-agnostic — the same fields cover both
## mysql and postgresql backends; the wire protocol is selected by
## `modules.sql.dialect`.
##
## @param externalDatabase.host Database host. Empty means "no external database" (combine with one of the bundled subcharts or with `modules.sql.dialect: sqlite`).
## @param externalDatabase.port Database port number. Leave empty to let the chart pick a dialect-appropriate default (`3306` for mysql, `5432` for postgresql).
## @param externalDatabase.user Non-root username for FreeRADIUS
## @param externalDatabase.password Password for the non-root username for FreeRADIUS
## @param externalDatabase.database FreeRADIUS database name
## @param externalDatabase.existingSecret Name of an existing secret resource containing the database credentials
## @param externalDatabase.existingSecretPasswordKey Key inside `externalDatabase.existingSecret` that holds the password
##
externalDatabase:
  host: ""
  port: ""
  user: freeradius_user
  database: freeradius_db
  password: ""
  existingSecret: ""
  existingSecretPasswordKey: ""

## Redis chart configuration. Enable to bundle a Bitnami Redis as the backend
## for the `rlm_redis` / `rlm_cache` module (`modules.redis.enabled`). When
## enabled, `modules.redis.server` is auto-pointed at the bundled
## `<release>-redis-master` Service (via the `freeradius.redis.host` helper) and,
## when `redis.auth.enabled`, the password is injected into the module from the
## subchart's Secret as `$ENV{FREERADIUS_MODS_REDIS_PASSWORD}`.
## ref: https://github.com/bitnami/charts/blob/master/bitnami/redis/values.yaml
##
## @param redis.enabled Switch to enable or disable the bundled Redis subchart. Pairs with `modules.redis.enabled`.
## @param redis.architecture Redis architecture (`standalone` or `replication`). The module targets the `-master` Service either way.
## @param redis.auth.enabled Enable password authentication on the bundled Redis (Bitnami default). The chart wires the password into the redis module automatically.
## @param redis.auth.password Password for the bundled Redis (auto-generated by the subchart when empty).
## @param redis.auth.existingSecret Name of an existing Secret holding the Redis password. Overrides the subchart-managed Secret.
## @param redis.auth.existingSecretPasswordKey Key inside the Secret that holds the password (Bitnami default `redis-password`).
##
redis:
  enabled: false
  global:
    security:
      allowInsecureImages: true
  image:
    registry: public.ecr.aws
    repository: bitnami/redis
    tag: 8.6.2
  architecture: standalone
  auth:
    enabled: true
    password: ""
    existingSecret: ""
    existingSecretPasswordKey: ""
  master:
    kind: Deployment
    persistence:
      enabled: false
  ## Bitnami's redis subchart declares `metrics.extraArgs` as a map (not a
  ## list) — exporter CLI flags rendered as `--key=value` pairs. Re-stating
  ## the shape here makes the contract explicit so downstream overrides don't
  ## accidentally hand a list (which triggers `coalesce.go: cannot overwrite
  ## table with non table for redis.metrics.extraArgs (map[])`).
  ##
  metrics:
    extraArgs: {}

## Database bootstrap. Loads the rlm_sql schema (`files/schema/<dialect>.sql`)
## into the configured database via an init container on the FreeRADIUS pod
## before the main container starts. Idempotent — every shipped schema uses
## `CREATE TABLE IF NOT EXISTS`, so re-runs on restart are no-ops.
##
## Works with any backend selection: bundled `mariadb.enabled: true` /
## `postgresql.enabled: true` (waits for the subchart's Service to come up) or
## `externalDatabase.*` (assumes the external DB is reachable). Skipped when
## `modules.sql.dialect: sqlite` (the SQLite driver creates the schema at
## first write).
##
bootstrap:
  database:
    ## @param bootstrap.database.enabled Run the schema bootstrap init container before the FreeRADIUS container starts. Disable if your database is managed/bootstrapped out-of-band.
    ##
    enabled: true
    ## @param bootstrap.database.schemaConfigMap Name of an existing ConfigMap (must contain `schema.sql`) used in place of the chart-rendered `<fullname>-db-schema`. Use this to ship a customised schema without forking the chart.
    ##
    schemaConfigMap: ""
    ## Init container image used to apply the bundled `files/schema/<dialect>.sql`
    ## into the configured database. Per-dialect canonical defaults live in
    ## the `freeradius.bootstrap.database.image` helper (currently
    ## `bitnami/mariadb:11` for mysql and `bitnami/postgresql:17` for
    ## postgresql — each ships the matching CLI). Override field-by-field
    ## below: any non-empty value here wins, empty values fall through to
    ## the dialect default. Use this to switch registries (Bitnami mirror,
    ## ECR, etc.), pin a specific tag, or point at a slimmer image
    ## (`postgres:17-alpine`, `mariadb:11-noble`). Sqlite skips bootstrap
    ## entirely so this image is unused in that case.
    ##
    ## @param bootstrap.database.image.registry Image registry (empty = dialect default — `docker.io`)
    ## @param bootstrap.database.image.repository Image repository (empty = dialect default — `bitnami/mariadb` or `bitnami/postgresql`)
    ## @param bootstrap.database.image.tag Image tag (empty = dialect default — `11` or `17`)
    ## @param bootstrap.database.image.digest Image digest, takes precedence over `tag` when set
    ## @param bootstrap.database.image.pullPolicy Image pull policy
    ## @param bootstrap.database.image.pullSecrets Image pull secrets
    ##
    image:
      registry: ""
      repository: ""
      tag: ""
      digest: ""
      pullPolicy: IfNotPresent
      pullSecrets: []
    ## @param bootstrap.database.resources Resources requested by the bootstrap init container
    ##
    resources:
      limits: {}
      requests: {}
    ## @param bootstrap.database.waitTimeout Seconds to wait for the database to accept connections before failing. The init container retries every 2s.
    ##
    waitTimeout: 120
  ## `volumePermissions` init container — runs as root to chown the persistence
  ## mount point to `containerSecurityContext.runAsUser:podSecurityContext.fsGroup`.
  ## Only needed when the storage class doesn't honor `fsGroup` or you're
  ## bringing a pre-existing volume.
  ##
  volumePermissions:
    ## @param bootstrap.volumePermissions.enabled Enable the init container that chowns the PV mount point
    ##
    enabled: true
    ## @param bootstrap.volumePermissions.image.registry Image registry for the init container
    ## @param bootstrap.volumePermissions.image.repository Image repository
    ## @param bootstrap.volumePermissions.image.tag Image tag
    ## @param bootstrap.volumePermissions.image.pullPolicy Image pull policy
    ## @param bootstrap.volumePermissions.image.pullSecrets Image pull secrets
    ##
    image:
      registry: public.ecr.aws
      repository: bitnami/os-shell
      tag: "12"
      digest: ""
      pullPolicy: IfNotPresent
      pullSecrets: []
    ## @param bootstrap.volumePermissions.resources.limits Resource limits for the init container
    ## @param bootstrap.volumePermissions.resources.requests Requested resources for the init container
    ##
    resources:
      limits: {}
      requests: {}
    ## @param bootstrap.volumePermissions.securityContext.runAsUser UID for the init container. Use `0` (root) so chown actually works; or `auto` for OpenShift's dynamic SCC.
    ##
    securityContext:
      runAsUser: 0

## Utility deployment that ships `radtest` / `radclient` (Debian
## `freeradius-utils` package). The chart's main image
## (`freeradius/freeradius-server`) strips client tools, so this is the
## convenient way to send RADIUS Access-Requests against the in-cluster
## service for debugging Keycloak / EAP / SQL flows. The pod runs
## `sleep infinity` after installing the package on first start; usage:
##
##   kubectl exec -it deploy/<release>-freeradius-radtest -- bash
##   # env vars RADIUS_HOST / RADIUS_AUTH_PORT / RADIUS_SECRET are
##   # pre-populated; supply the user/password (Keycloak creds):
##   radtest <user> <pass> $RADIUS_HOST $RADIUS_NAS_PORT $RADIUS_SECRET
##
## Disabled by default. Intended for short-lived debugging — DO NOT use
## as a production traffic generator.
##
radtest:
  ## @param radtest.enabled Render the radtest utility Deployment.
  ##
  enabled: false
  ## @param radtest.replicaCount Number of radtest pods.
  ##
  replicaCount: 1
  ## Container image for the radtest utility pod. Defaults to `2stacks/radtest`
  ## — Alpine-based, ~8 MB, ships `radtest` / `radclient` preinstalled (no
  ## startup install step). Override to any image that already includes the
  ## FreeRADIUS client tools if you want to pin a specific client version.
  ##
  ## @param radtest.image.registry Image registry
  ## @param radtest.image.repository Image repository
  ## @param radtest.image.tag Image tag
  ## @param radtest.image.digest Image digest (overrides `tag` when set)
  ## @param radtest.image.pullPolicy Image pull policy
  ## @param radtest.image.pullSecrets Image pull secrets
  ##
  image:
    registry: docker.io
    repository: 2stacks/radtest
    tag: latest
    digest: ""
    pullPolicy: IfNotPresent
    pullSecrets: []
  ## @param radtest.secret RADIUS shared secret used by the pre-populated `$RADIUS_SECRET` env var. Must match the entry in `clients.conf` for the source IP the radtest pod uses (cluster-internal — typically matches the loopback/in-cluster client entry).
  ##
  secret: password
  ## @param radtest.extraEnvVars Extra environment variables for the radtest container.
  ##
  extraEnvVars: []
  ## @param radtest.resources Set container requests/limits. Leave empty to fall back to `resourcesPreset`.
  ## @param radtest.resourcesPreset Preset shorthand (`none`, `nano`, `micro`, `small`, ...) when `resources` is empty.
  ##
  resources: {}
  resourcesPreset: nano
  ## Radtest container security context. Tight by default (PSS "restricted"):
  ## radtest/radclient only open a UDP socket, read stdin, and write to
  ## stdout/stderr — no privileged ops, no on-disk state. Safe with a
  ## non-root UID, dropped caps and a read-only root filesystem.
  ##
  ## @param radtest.containerSecurityContext.enabled Apply the security context to the radtest container.
  ## @param radtest.containerSecurityContext.runAsUser UID for the radtest container.
  ## @param radtest.containerSecurityContext.runAsGroup GID for the radtest container.
  ## @param radtest.containerSecurityContext.runAsNonRoot Reject containers that try to run as root.
  ## @param radtest.containerSecurityContext.allowPrivilegeEscalation Block child processes from gaining more privileges than the parent.
  ## @param radtest.containerSecurityContext.readOnlyRootFilesystem Mount the container's root filesystem as read-only.
  ## @param radtest.containerSecurityContext.capabilities Linux capabilities to add/drop on the radtest container.
  ## @param radtest.containerSecurityContext.seccompProfile Seccomp profile applied to the radtest container.
  ##
  containerSecurityContext:
    enabled: true
    runAsUser: 65534
    runAsGroup: 65534
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: ["ALL"]
    seccompProfile:
      type: RuntimeDefault
  ## @param radtest.podAnnotations Annotations for the radtest pod.
  ## @param radtest.podLabels Labels for the radtest pod.
  ##
  podAnnotations: {}
  podLabels: {}
  ## @param radtest.nodeSelector Node selector for the radtest pod.
  ## @param radtest.tolerations Tolerations for the radtest pod.
  ## @param radtest.affinity Affinity rules for the radtest pod.
  ## @param radtest.priorityClassName Priority class for the radtest pod.
  ##
  nodeSelector: {}
  tolerations: []
  affinity: {}
  priorityClassName: ""
