{{- /*
(c) 2026 Firmansyah Nainggolan <firmansyah@nainggolan.id>. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.sites.coa.enabled (not .Values.sites.coa.existingConfigMap) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-sites-coa" (include "st-common.names.fullname" .) }}
  namespace: {{ include "st-common.names.namespace" $ | quote }}
  labels: {{- include "st-common.labels.standard" (dict "customLabels" $.Values.commonLabels "context" $) | nindent 4 }}
    app.kubernetes.io/component: freeradius
  {{- if $.Values.commonAnnotations }}
  annotations: {{- include "st-common.tplvalues.render" (dict "value" $.Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
data:
  coa: |
    # coa virtual server (Change-of-Authorization / Disconnect-Request)

    server coa {

    	listen {
    		type = coa
    		ipaddr = {{ .Values.sites.coa.listen.ipaddr }}
    		port = {{ .Values.containerPorts.coa }}
    	}

    	# Processed for *both* CoA-Request and Disconnect-Request packets.
    	recv-coa {
    		suffix
    		ok
    	}

    	# Processed for *both* CoA-Request and Disconnect-Request packets.
    	send-coa {
    		ok
    	}
    }
{{- end }}
