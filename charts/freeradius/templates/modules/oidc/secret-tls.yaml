{{- /*
(c) 2026 Firmansyah Nainggolan <firmansyah@nainggolan.id>. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{/*
Per-instance chart-rendered Secrets holding the OIDC IdP TLS CA bundle
as `ca.crt`. One Secret per instance whose `tls.caCert` is non-empty AND
whose `tls.existingSecret` is unset (BYO).

  default → `<fullname>-oidc-ca`           key: `ca.crt`
  <name>  → `<fullname>-oidc-<name>-ca`    key: `ca.crt`

Instances using `tls.existingSecret` skip rendering and reference the
BYO Secret directly from Application.yaml. Instances with neither set
fall back to the image's system CA bundle.
*/}}

{{- if .Values.modules.oidc.enabled }}
{{- $resolved := include "freeradius.oidc.resolveInstances" . | fromYaml }}
{{- range $name, $cfg := $resolved.instances }}
{{- if include "freeradius.oidc.tls.createSecret" (dict "instance" $cfg) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "freeradius.oidc.tls.secretName" (dict "name" $name "instance" $cfg "context" $) }}
  namespace: {{ include "st-common.names.namespace" $ | quote }}
  labels: {{- include "st-common.labels.standard" (dict "customLabels" $.Values.commonLabels "context" $) | nindent 4 }}
    app.kubernetes.io/component: freeradius
    app.firmansyah.id/oidc-instance: {{ $name | quote }}
  {{- if $.Values.commonAnnotations }}
  annotations: {{- include "st-common.tplvalues.render" (dict "value" $.Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
  ca.crt: |
{{ $cfg.tls.caCert | indent 4 }}
{{- end }}
{{- end }}
{{- end }}
