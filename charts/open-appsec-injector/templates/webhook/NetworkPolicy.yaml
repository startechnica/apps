{{- /*
(c) 2026 Firmansyah Nainggolan <firmansyah@nainggolan.id>. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if .Values.webhook.networkPolicy.enabled }}
apiVersion: {{ template "st-common.capabilities.networkPolicy.apiVersion" . }}
kind: NetworkPolicy
metadata:
  name: {{ include "open-appsec.webhook.fullname" . }}
  namespace: {{ include "st-common.names.namespace" . | quote }}
  labels: {{- include "st-common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: webhook
    app.kubernetes.io/part-of: open-appsec
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "st-common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  podSelector:
    matchLabels: {{- include "st-common.labels.matchLabels" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 6 }}
      app.kubernetes.io/component: webhook
  policyTypes:
    - Ingress
    - Egress
  {{- if .Values.webhook.networkPolicy.allowExternalEgress }}
  egress:
    - {}
  {{- else }}
  egress:
    # Allow DNS resolution
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # Allow egress to the K8s API server — required by secretgen.py to patch
    # the MutatingWebhookConfiguration's caBundle at startup.
    - ports:
        - port: 443
          protocol: TCP
        - port: 6443
          protocol: TCP
    {{- if .Values.webhook.networkPolicy.extraEgress }}
    {{- include "st-common.tplvalues.render" ( dict "value" .Values.webhook.networkPolicy.extraEgress "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
  ingress:
    # Allow inbound webhook admission calls from the K8s API server (on :443).
    - ports:
        - port: {{ .Values.webhook.containerPorts.https }}
          protocol: TCP
      {{- if not .Values.webhook.networkPolicy.allowExternal }}
      from:
        - podSelector:
            matchLabels:
              {{ include "open-appsec.webhook.fullname" . }}-client: "true"
        {{- /*
        Mirror the ClusterRoleBinding subject scope onto the NetworkPolicy ingress:
        the namespaces listed in .Values.allowedNamespaces are the ones whose pods
        we expect to call the webhook (typically injected sidecars). Skipped on
        ["*"] (allowExternal already covers it) or [] (no extra ingress needed).
        */ -}}
        {{- $allowed := .Values.allowedNamespaces | default (list) }}
        {{- if and (not (has "*" $allowed)) (not (empty $allowed)) }}
        - namespaceSelector:
            matchExpressions:
              - key: kubernetes.io/metadata.name
                operator: In
                values:
                  {{- toYaml $allowed | nindent 18 }}
        {{- end }}
        {{- if or .Values.webhook.networkPolicy.ingressNSMatchLabels .Values.webhook.networkPolicy.ingressNSPodMatchLabels }}
        - namespaceSelector:
            matchLabels:
              {{- if .Values.webhook.networkPolicy.ingressNSMatchLabels }}
                {{- range $key, $value := .Values.webhook.networkPolicy.ingressNSMatchLabels }}
                {{ $key | quote }}: {{ $value | quote }}
                {{- end }}
              {{- else }}
                {}
              {{- end }}
          {{- if .Values.webhook.networkPolicy.ingressNSPodMatchLabels }}
          podSelector:
            matchLabels:
              {{- range $key, $value := .Values.webhook.networkPolicy.ingressNSPodMatchLabels }}
              {{ $key | quote }}: {{ $value | quote }}
              {{- end }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- if .Values.metrics.enabled }}
    # Allow Prometheus scrapes for metrics
    - ports:
        - port: {{ .Values.webhook.containerPorts.metrics }}
          protocol: TCP
      {{- if not .Values.webhook.networkPolicy.metrics.allowExternal }}
      from:
        {{- if or .Values.webhook.networkPolicy.metrics.ingressNSMatchLabels .Values.webhook.networkPolicy.metrics.ingressNSPodMatchLabels }}
        - namespaceSelector:
            matchLabels:
              {{- if .Values.webhook.networkPolicy.metrics.ingressNSMatchLabels }}
                {{- range $key, $value := .Values.webhook.networkPolicy.metrics.ingressNSMatchLabels }}
                {{ $key | quote }}: {{ $value | quote }}
                {{- end }}
              {{- else }}
                {}
              {{- end }}
          {{- if .Values.webhook.networkPolicy.metrics.ingressNSPodMatchLabels }}
          podSelector:
            matchLabels:
              {{- range $key, $value := .Values.webhook.networkPolicy.metrics.ingressNSPodMatchLabels }}
              {{ $key | quote }}: {{ $value | quote }}
              {{- end }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- end }}
    {{- if .Values.webhook.networkPolicy.extraIngress }}
    {{- include "st-common.tplvalues.render" ( dict "value" .Values.webhook.networkPolicy.extraIngress "context" $ ) | nindent 4 }}
    {{- end }}
{{- end }}
