{{- /*
(c) 2025 Firmansyah Nainggolan <firmansyah@nainggolan.id>. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.agent.persistence.enabled (not .Values.agent.persistence.data.existingClaim) }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "open-appsec.agent-data.pvClaim" . }}
  namespace: {{ include "st-common.names.namespace" . | quote }}
  labels: {{- include "st-common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: agent
  {{- if or .Values.agent.persistence.data.annotations .Values.commonAnnotations }}
  {{- $annotations := include "st-common.tplvalues.merge" (dict "values" (list .Values.agent.persistence.data.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "st-common.tplvalues.render" (dict "value" $annotations "context" .) | nindent 4 }}
  {{- end }}
spec:
  accessModes:
  {{- range .Values.agent.persistence.data.accessModes }}
    - {{ . | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.agent.persistence.data.size | quote }}
  {{- if .Values.agent.persistence.data.selector }}
  selector: {{- include "st-common.tplvalues.render" (dict "value" .Values.agent.persistence.data.selector "context" .) | nindent 4 }}
  {{- end }}
  {{- if .Values.agent.persistence.data.dataSource }}
  dataSource: {{- include "st-common.tplvalues.render" (dict "value" .Values.agent.persistence.data.dataSource "context" .) | nindent 4 }}
  {{- end }}
  {{- include "st-common.storage.class" (dict "persistence" .Values.agent.persistence.data "global" .Values.global) -}}
{{- end }}
